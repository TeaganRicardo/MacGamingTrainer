diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/Backend/core/protocol.py /mnt/data/MacGamingTrainer_v0.17.7/Backend/core/protocol.py
--- /mnt/data/MacGamingTrainer_v0.17.6/Backend/core/protocol.py	2026-09-17 14:55:13.516739048 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/Backend/core/protocol.py	2026-09-17 15:02:50.120565643 +0000
@@ -6,7 +6,7 @@
 # module has its own independent protocol/schema version exposed alongside it.
 HOST_PROTOCOL_VERSION = 5
 PROTOCOL_VERSION = HOST_PROTOCOL_VERSION  # compatibility alias for older module code
-APP_BACKEND_VERSION = '0.17.6'
+APP_BACKEND_VERSION = '0.17.7'
 
 
 from core.adapter import GameAdapter
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/Info.plist /mnt/data/MacGamingTrainer_v0.17.7/Info.plist
--- /mnt/data/MacGamingTrainer_v0.17.6/Info.plist	2026-09-17 14:55:13.516599520 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/Info.plist	2026-09-17 15:02:50.120505443 +0000
@@ -17,9 +17,9 @@
 	<key>CFBundlePackageType</key>
 	<string>APPL</string>
 	<key>CFBundleShortVersionString</key>
-	<string>0.17.6</string>
+	<string>0.17.7</string>
 	<key>CFBundleVersion</key>
-	<string>29</string>
+	<string>30</string>
 	<key>LSMinimumSystemVersion</key>
 	<string>14.0</string>
 	<key>NSHighResolutionCapable</key>
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/RELEASE_NOTES_v0.17.7.md /mnt/data/MacGamingTrainer_v0.17.7/RELEASE_NOTES_v0.17.7.md
--- /mnt/data/MacGamingTrainer_v0.17.6/RELEASE_NOTES_v0.17.7.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/RELEASE_NOTES_v0.17.7.md	2026-09-17 15:02:50.297826652 +0000
@@ -0,0 +1,26 @@
+# v0.17.7
+
+App version: **0.17.7**  
+Build: **30**  
+Host protocol: **v5 (unchanged)**  
+Hades II module protocol: **v4 (unchanged)**  
+Lua runtime revision: **19 (unchanged)**
+
+## Connection watchdog correction
+
+The v0.17 reliability pass introduced a 12-second timeout for Hades II `connect`. Historical successful target-Mac logs show LLDB attach + symbol validation + first Lua bootstrap can legitimately take more than 50 seconds. The watchdog therefore terminated healthy connections before they completed.
+
+Timeout policy is now:
+
+- scan: 15 s
+- status: 8 s
+- connect: 90 s
+- disconnect: 12 s
+
+This is intentionally a Hades-local policy; Core request timeout enforcement remains unchanged.
+
+## Why the UI looked unchanged/green
+
+v0.17.5–0.17.6 primarily moved the existing visual components into Core without redesigning them, so a visual diff was expected to be small. The green feature switches shown while testing are the pre-existing **detached/deferred desired-state** color. They indicate the feature is remembered as enabled while there is no confirmed live runtime connection. The failed 12-second attach made that state persist.
+
+The lower boon controls use native compact switches in the pre-decoupling baseline as well, so they intentionally remain visually different from the 44×25 feature switches.
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/Sources/Hades2/Hades2API.swift /mnt/data/MacGamingTrainer_v0.17.7/Sources/Hades2/Hades2API.swift
--- /mnt/data/MacGamingTrainer_v0.17.6/Sources/Hades2/Hades2API.swift	2026-09-17 12:12:24.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/Sources/Hades2/Hades2API.swift	2026-09-17 15:02:50.120369439 +0000
@@ -112,9 +112,18 @@
 
     var timeout: TimeInterval {
         switch self {
-        case .scan, .status:
-            return 4.0
-        case .connect, .disconnect:
+        case .scan:
+            // pgrep is normally quick, but historical successful scans can exceed
+            // four seconds while Steam/game process state is settling.
+            return 15.0
+        case .status:
+            return 8.0
+        case .connect:
+            // LLDB attach + symbol validation + first Lua bootstrap is the slow
+            // path. Real successful Hades II connections have taken >50 s on
+            // the target Mac; a 12 s watchdog incorrectly killed healthy attach.
+            return 90.0
+        case .disconnect:
             return 12.0
         case .launch:
             return 10.0
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/tests/test_backend_core_round12.py /mnt/data/MacGamingTrainer_v0.17.7/tests/test_backend_core_round12.py
--- /mnt/data/MacGamingTrainer_v0.17.6/tests/test_backend_core_round12.py	2026-09-17 14:55:13.516801392 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/tests/test_backend_core_round12.py	2026-09-17 15:02:50.121222277 +0000
@@ -34,7 +34,7 @@
 
 
 assert HOST_PROTOCOL_VERSION == PROTOCOL_VERSION == 5
-assert APP_BACKEND_VERSION == '0.17.6'
+assert APP_BACKEND_VERSION == '0.17.7'
 assert any(row['id'] == 'hades2' and row['protocolVersion'] == 4 for row in available_games())
 
 context = GameAdapterContext(
@@ -46,7 +46,7 @@
 hello = router.handle({'id':'hello','command':'hello','params':{}})
 assert hello['ok'] and hello['protocolVersion'] == 5
 assert hello['gameID'] == 'fake' and hello['moduleProtocolVersion'] == 7
-assert hello['result']['backendVersion'] == '0.17.6'
+assert hello['result']['backendVersion'] == '0.17.7'
 reply = router.handle({'id':'a','command':'poke','params':{'x':1}})
 assert reply['ok'] and reply['result']['command'] == 'poke'
 assert router.handle({'id':'a','command':'poke','params':{'x':1}}) == reply
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/tests/test_hades2_timeout_policy_v0177.py /mnt/data/MacGamingTrainer_v0.17.7/tests/test_hades2_timeout_policy_v0177.py
--- /mnt/data/MacGamingTrainer_v0.17.6/tests/test_hades2_timeout_policy_v0177.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/tests/test_hades2_timeout_policy_v0177.py	2026-09-17 15:02:50.296945231 +0000
@@ -0,0 +1,18 @@
+from pathlib import Path
+import re
+
+ROOT = Path(__file__).resolve().parents[1]
+api = (ROOT / 'Sources/Hades2/Hades2API.swift').read_text()
+
+# Connect must allow the empirically slow LLDB attach/bootstrap path. A prior
+# 12 s watchdog killed healthy connections and left the UI in detached/deferred
+# state (green desired switches, no live catalog).
+assert re.search(r'case \.connect:\s*(?:.|\n)*?return 90\.0', api)
+assert re.search(r'case \.scan:\s*(?:.|\n)*?return 15\.0', api)
+assert re.search(r'case \.status:\s*(?:.|\n)*?return 8\.0', api)
+assert re.search(r'case \.disconnect:\s*(?:.|\n)*?return 12\.0', api)
+
+# Guard against re-introducing the coupled 12 s connect/disconnect case.
+assert 'case .connect, .disconnect' not in api
+
+print('hades2_timeout_policy_v0177_ok')
diff -ruN '--exclude=VALIDATION_v0.17.7.txt' '--exclude=CHANGES_v0.17.6_to_v0.17.7.diff' /mnt/data/MacGamingTrainer_v0.17.6/tests/test_round17_static.py /mnt/data/MacGamingTrainer_v0.17.7/tests/test_round17_static.py
--- /mnt/data/MacGamingTrainer_v0.17.6/tests/test_round17_static.py	2026-09-17 14:55:13.516847461 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.7/tests/test_round17_static.py	2026-09-17 15:03:41.197223437 +0000
@@ -23,7 +23,7 @@
 
 # Hades chooses command-specific timeout policy; Core remains game agnostic.
 assert 'var timeout: TimeInterval' in api
-for value in ['return 4.0', 'return 12.0', 'return 30.0']:
+for value in ['return 15.0', 'return 8.0', 'return 90.0', 'return 12.0', 'return 30.0']:
     assert value in api
 assert 'Hades' not in client and 'Hades' not in process and 'Hades' not in session
 
@@ -45,7 +45,7 @@
 
 with (ROOT / 'Info.plist').open('rb') as handle:
     plist = plistlib.load(handle)
-assert plist['CFBundleShortVersionString'] == '0.17.6'
-assert plist['CFBundleVersion'] == '29'
+assert plist['CFBundleShortVersionString'] == '0.17.7'
+assert plist['CFBundleVersion'] == '30'
 
 print('round17_static_ok')
