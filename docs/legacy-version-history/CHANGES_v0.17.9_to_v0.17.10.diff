diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/BUILDING.md /mnt/data/MacGamingTrainer_v0.17.10/BUILDING.md
--- /mnt/data/MacGamingTrainer_v0.17.9/BUILDING.md	2026-09-17 16:35:53.125660205 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/BUILDING.md	2026-09-17 16:45:11.474932052 +0000
@@ -1,4 +1,4 @@
-# Build v0.17.9
+# Build v0.17.10
 
 当前唯一支持的构建方式是手动运行 `build.sh`。此前实验性的 Finder 双击构建 App 已暂时移除，因为在下载目录中会受到 macOS Finder 权限与 bundle 路径解析影响。
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/Backend/core/protocol.py /mnt/data/MacGamingTrainer_v0.17.10/Backend/core/protocol.py
--- /mnt/data/MacGamingTrainer_v0.17.9/Backend/core/protocol.py	2026-09-17 16:18:39.507976871 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/Backend/core/protocol.py	2026-09-17 16:45:11.473980368 +0000
@@ -6,7 +6,7 @@
 # module has its own independent protocol/schema version exposed alongside it.
 HOST_PROTOCOL_VERSION = 5
 PROTOCOL_VERSION = HOST_PROTOCOL_VERSION  # compatibility alias for older module code
-APP_BACKEND_VERSION = '0.17.9'
+APP_BACKEND_VERSION = '0.17.10'
 
 
 from core.adapter import GameAdapter
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/runtime/hades.lua /mnt/data/MacGamingTrainer_v0.17.10/Backend/games/hades2/runtime/hades.lua
--- /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/runtime/hades.lua	2026-09-17 16:15:15.604519501 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/Backend/games/hades2/runtime/hades.lua	2026-09-17 16:45:45.099180870 +0000
@@ -6,13 +6,13 @@
 end
 if type(UpdateTimers) ~= "function" then error("Unsupported game runtime: missing UpdateTimers") end
 local previousModule = __MacGamingTrainerV1
-if previousModule and previousModule.revision ~= 20 then
+if previousModule and previousModule.revision ~= 21 then
   previousModule.dispatch("cleanup")
   __MacGamingTrainerV1 = nil
 end
 if __MacGamingTrainerV1 == nil then
   local M = {
-    version = 1, revision = 20, damageMultiplier = 2, damageEnabled = false,
+    version = 1, revision = 21, damageMultiplier = 2, damageEnabled = false,
     gameSpeed = 1, gameSpeedActive = false, gameSpeedCallStyle = nil,
     gameSpeedMethod = nil, gameSpeedAppliedValue = nil,
     godMode = false, infiniteHealth = false, infiniteMana = false,
@@ -20,7 +20,7 @@
     moneyMultiplier = 2, moneyMultiplierEnabled = false,
     resourceMultiplier = 2, resourceMultiplierEnabled = false,
     boonRarityTarget = "Epic", boonRarityMultiplier = 1, boonAllowLegendary = true, boonAllowDuo = true,
-    boonChoiceCount = 3, nextRoomReward = nil,
+    boonChoiceCount = 3, nextRoomReward = nil, nextRoomRewardOriginRoom = nil, nextRoomRewardPatchedDoors = 0, nextRoomRewardPatchedValue = nil, nextRoomRewardPatchRoom = nil,
     desiredFeatures = {
       godMode = false, infiniteHealth = false, infiniteMana = false, damageEnabled = false,
       instantCastCooldown = false, hexAlwaysReady = false, infiniteAmmo = false, autoMiniGames = false, gardenQoL = false, boonRarityEnabled = false, boonChoiceEnabled = false,
@@ -579,6 +579,11 @@
   end
   local function releaseNextRoomReward()
     releaseHook("ChooseRoomReward")
+    releaseHook("StartRoom")
+    M.nextRoomRewardOriginRoom = nil
+    M.nextRoomRewardPatchedDoors = 0
+    M.nextRoomRewardPatchedValue = nil
+    M.nextRoomRewardPatchRoom = nil
   end
   local function releaseEconomyRuntime()
     M.moneyMultiplierEnabled, M.resourceMultiplierEnabled = false, false
@@ -1664,7 +1669,8 @@
         boonRarityHooks = { chances = owns("GetRarityChances"), options = owns("SetTraitsOnLoot") },
         boonChoiceHooks = { calc = owns("CalcNumLootChoices"), total = owns("GetTotalLootChoices") },
         gardenHooks = { plant = owns("GardenPlantSeed"), harvest = owns("UseGardenPlot") },
-        nextRoomRewardHook = owns("ChooseRoomReward"),
+        nextRoomRewardHook = owns("ChooseRoomReward") and owns("StartRoom"),
+        nextRoomRewardPatchedDoors = M.nextRoomRewardPatchedDoors,
       },
     }
   end
@@ -2037,41 +2043,113 @@
     M.boonChoiceEnabled = true
   end
 
-  local function installNextRoomReward()
-    requireFunctions("next room reward", { "ChooseRoomReward" })
-    if owns("ChooseRoomReward") then return end
-    local directReward = {
-      RoomMoneyDrop=true, MetaCurrencyDrop=true, MetaCardPointsCommonDrop=true, MemPointsCommonDrop=true,
-      MaxHealthDrop=true, MaxManaDrop=true, StackUpgrade=true, WeaponUpgrade=true, HermesUpgrade=true,
-    }
-    installHook("ChooseRoomReward", function(original, run, room, rewardStoreName, previouslyChosenRewards, args, ...)
-      local native = original(run, room, rewardStoreName, previouslyChosenRewards, args, ...)
-      local wanted = M.nextRoomReward
-      if wanted == nil or type(room) ~= "table" or type(native) ~= "string" then return native end
-      if type(room.Encounter) == "table" and type(room.Encounter.Name) == "string" and string.find(room.Encounter.Name, "Boss", 1, true) then return native end
-      local nativeKnown = native == "Boon" or directReward[native] == true
-        or (type(ConsumableData) == "table" and type(ConsumableData[native]) == "table")
-        or (type(LootData) == "table" and type(LootData[native]) == "table")
-      if not nativeKnown then return native end
+  local nextRoomDirectRewards = {
+    RoomMoneyDrop=true, MetaCurrencyDrop=true, MetaCardPointsCommonDrop=true, MemPointsCommonDrop=true,
+    MaxHealthDrop=true, MaxManaDrop=true, StackUpgrade=true, WeaponUpgrade=true, HermesUpgrade=true,
+  }
 
-      local rewardType, forceLoot = wanted, nil
-      if not directReward[wanted] then
-        if type(LootData) ~= "table" or type(LootData[wanted]) ~= "table" then return native end
-        rewardType, forceLoot = "Boon", wanted
-      end
+  local function nextRoomRewardSpec(wanted)
+    if type(wanted) ~= "string" then return nil, nil end
+    if nextRoomDirectRewards[wanted] then return wanted, nil end
+    if type(LootData) == "table" and type(LootData[wanted]) == "table" then return "Boon", wanted end
+    return nil, nil
+  end
 
-      room.Reward = { Name = rewardType }
-      room.ChosenRewardType = rewardType
-      if forceLoot ~= nil then
-        room.Reward.ForceLootName = forceLoot
-        room.ForceLootName = forceLoot
-      else
-        room.ForceLootName = nil
+  local function ordinaryDoorReward(room, native)
+    if type(room) ~= "table" or type(native) ~= "string" then return false end
+    if room.NoReward or room.DeferReward then return false end
+    if native == "Story" or native == "Shop" or native == "Empty" or native == "Devotion" then return false end
+    if type(room.Encounter) == "table" and type(room.Encounter.Name) == "string"
+        and string.find(room.Encounter.Name, "Boss", 1, true) then return false end
+    return native == "Boon" or nextRoomDirectRewards[native] == true
+      or (type(ConsumableData) == "table" and type(ConsumableData[native]) == "table")
+      or (type(LootData) == "table" and type(LootData[native]) == "table")
+  end
+
+  local function forceNextRoomReward(room, wanted)
+    local rewardType, forceLoot = nextRoomRewardSpec(wanted)
+    if rewardType == nil or type(room) ~= "table" then return nil, nil end
+    room.Reward = { Name = rewardType }
+    room.ChosenRewardType = rewardType
+    if forceLoot ~= nil then
+      room.Reward.ForceLootName = forceLoot
+      room.ForceLootName = forceLoot
+    else
+      room.ForceLootName = nil
+    end
+    return rewardType, forceLoot
+  end
+
+  local function patchOfferedNextRoomDoors()
+    local wanted = M.nextRoomReward
+    if wanted == nil or type(MapState) ~= "table" or type(MapState.OfferedExitDoors) ~= "table" then
+      M.nextRoomRewardPatchedDoors = 0
+      return 0
+    end
+    local patched = 0
+    for doorId, door in pairs(MapState.OfferedExitDoors) do
+      local room = type(door) == "table" and door.Room or nil
+      local native = type(room) == "table" and room.ChosenRewardType or nil
+      if ordinaryDoorReward(room, native) then
+        local rewardType, forceLoot = forceNextRoomReward(room, wanted)
+        if rewardType ~= nil then
+          patched = patched + 1
+          if type(CurrentRun) == "table" and type(CurrentRun.CurrentRoom) == "table" then
+            CurrentRun.CurrentRoom.OfferedRewards = CurrentRun.CurrentRoom.OfferedRewards or {}
+            CurrentRun.CurrentRoom.OfferedRewards[doorId] = { Type = rewardType, ForceLootName = forceLoot }
+          end
+          -- Existing previews were already materialized before the trainer command.
+          -- Reuse those world-UI objects instead of spawning duplicate icons.
+          if type(CreateDoorRewardPreview) == "function" and type(door.RewardPreviewIconIds) == "table"
+              and next(door.RewardPreviewIconIds) ~= nil then
+            pcall(CreateDoorRewardPreview, door, rewardType, forceLoot, nil, { ReUseIds = true })
+          end
+          if type(RefreshUseButton) == "function" and door.ObjectId ~= nil then
+            pcall(RefreshUseButton, door.ObjectId, door)
+          end
+        end
       end
-      M.nextRoomReward = nil
-      releaseHook("ChooseRoomReward")
-      return rewardType
-    end)
+    end
+    M.nextRoomRewardPatchedDoors = patched
+    return patched
+  end
+
+  local function installNextRoomReward()
+    requireFunctions("next room reward", { "ChooseRoomReward", "StartRoom" })
+    if M.nextRoomRewardOriginRoom == nil and type(CurrentRun) == "table" then
+      M.nextRoomRewardOriginRoom = CurrentRun.CurrentRoom
+    end
+    if not owns("ChooseRoomReward") then
+      installHook("ChooseRoomReward", function(original, run, room, rewardStoreName, previouslyChosenRewards, args, ...)
+        local native = original(run, room, rewardStoreName, previouslyChosenRewards, args, ...)
+        local wanted = M.nextRoomReward
+        -- The game chooses each offered exit independently. Keep the one-shot
+        -- armed across every door so the player's actual selection is forced.
+        if wanted == nil or type(args) ~= "table" or type(args.Door) ~= "table"
+            or not ordinaryDoorReward(room, native) then return native end
+        local rewardType = forceNextRoomReward(room, wanted)
+        return rewardType or native
+      end)
+    end
+    if not owns("StartRoom") then
+      installHook("StartRoom", function(original, currentRun, currentRoom, ...)
+        local result = original(currentRun, currentRoom, ...)
+        if M.nextRoomReward ~= nil then
+          local origin = M.nextRoomRewardOriginRoom
+          if origin == nil or currentRoom ~= origin then
+            M.nextRoomReward = nil
+            releaseNextRoomReward()
+          end
+        end
+        return result
+      end)
+    end
+    local patchRoom = type(CurrentRun) == "table" and CurrentRun.CurrentRoom or nil
+    if M.nextRoomRewardPatchedValue ~= M.nextRoomReward or M.nextRoomRewardPatchRoom ~= patchRoom then
+      patchOfferedNextRoomDoors()
+      M.nextRoomRewardPatchedValue = M.nextRoomReward
+      M.nextRoomRewardPatchRoom = patchRoom
+    end
   end
 
   local function installMana()
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/DEFERRED_REFACTOR_PLAN.md /mnt/data/MacGamingTrainer_v0.17.10/DEFERRED_REFACTOR_PLAN.md
--- /mnt/data/MacGamingTrainer_v0.17.9/DEFERRED_REFACTOR_PLAN.md	2026-09-17 16:35:53.177660481 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/DEFERRED_REFACTOR_PLAN.md	2026-09-17 16:45:11.475225922 +0000
@@ -1,4 +1,4 @@
-# Deferred work after v0.17.9
+# Deferred work after v0.17.10
 
 The framework-level corrective refactor and the first runtime-reliability pass are complete. Do not resume structural refactoring unless a concrete requirement demonstrates a missing host capability.
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/GAME_MODULES.md /mnt/data/MacGamingTrainer_v0.17.10/GAME_MODULES.md
--- /mnt/data/MacGamingTrainer_v0.17.9/GAME_MODULES.md	2026-09-17 16:35:53.581662607 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/GAME_MODULES.md	2026-09-17 16:45:11.475064110 +0000
@@ -1,4 +1,4 @@
-# Game module contract — v0.17.9
+# Game module contract — v0.17.10
 
 A normal new game should add only:
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/Info.plist /mnt/data/MacGamingTrainer_v0.17.10/Info.plist
--- /mnt/data/MacGamingTrainer_v0.17.9/Info.plist	2026-09-17 16:18:39.507838103 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/Info.plist	2026-09-17 16:45:11.473924675 +0000
@@ -17,9 +17,9 @@
 	<key>CFBundlePackageType</key>
 	<string>APPL</string>
 	<key>CFBundleShortVersionString</key>
-	<string>0.17.9</string>
+	<string>0.17.10</string>
 	<key>CFBundleVersion</key>
-	<string>32</string>
+	<string>33</string>
 	<key>LSMinimumSystemVersion</key>
 	<string>14.0</string>
 	<key>NSHighResolutionCapable</key>
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/REFACTOR_ASSESSMENT.md /mnt/data/MacGamingTrainer_v0.17.10/REFACTOR_ASSESSMENT.md
--- /mnt/data/MacGamingTrainer_v0.17.9/REFACTOR_ASSESSMENT.md	2026-09-17 16:35:53.001659556 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/REFACTOR_ASSESSMENT.md	2026-09-17 16:52:00.953410460 +0000
@@ -1,4 +1,4 @@
-# v0.17.9 architecture assessment
+# v0.17.10 architecture assessment
 
 ## Current verdict
 
@@ -10,7 +10,7 @@
 
 The current implementation meets that criterion for the tested module contract.
 
-v0.17.9 tightens the UI side of those boundaries without moving Hades business semantics into Core. Reusable visual controls now live in `Sources/Core/UI`; Hades keeps only page composition and the mapping from Hades runtime semantics to game-agnostic visual state. The shared controls preserve the proven Hades visual language instead of introducing another visual system. In v0.17.9 every switch path still converges on one shared native purple `TrainerToggleControl`; Hades owns no switch rendering logic. The switch now uses regular macOS control size. This release also restores current-game next-room reward identifiers, native Selene `GiveLoot` spawning, and lightweight live feature-state reconciliation without moving any Hades semantics into Core.
+v0.17.10 tightens the UI side of those boundaries without moving Hades business semantics into Core. Reusable visual controls now live in `Sources/Core/UI`; Hades keeps only page composition and the mapping from Hades runtime semantics to game-agnostic visual state. The shared controls preserve the proven Hades visual language instead of introducing another visual system. In v0.17.10 every switch path still converges on one shared native purple `TrainerToggleControl`; Hades owns no switch rendering logic. The switch now uses regular macOS control size. This release also keeps the current-game reward identifiers and native Selene `GiveLoot` spawning while removing debugger-boundary status polling; runtime state is refreshed only by explicit/user-driven requests. The next-room override remains entirely inside the Hades runtime module and now follows the game's per-exit reward flow.
 
 ## Framework responsibilities
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/RELEASE_NOTES_v0.17.10.md /mnt/data/MacGamingTrainer_v0.17.10/RELEASE_NOTES_v0.17.10.md
--- /mnt/data/MacGamingTrainer_v0.17.9/RELEASE_NOTES_v0.17.10.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/RELEASE_NOTES_v0.17.10.md	2026-09-17 16:45:11.474869369 +0000
@@ -0,0 +1,17 @@
+# v0.17.10
+
+App version: **0.17.10**  
+Build: **33**  
+Hades II Lua revision: **21**
+
+## Critical regression fixes
+
+- Removed v0.17.9's automatic 4-second Lua `status` polling. Each status request crosses the LLDB boundary and can pause Hades II; the live log showed repeated 0.2–1.3 s calls and an eventual boundary timeout. Runtime presentation now refreshes only on user-driven backend interactions or explicit refresh.
+- Save Manager no longer enters its blocking loading state while the staged-restore watcher performs its background `scan`. Actual backup/restore/delete/rename operations still block their own controls.
+- Reworked **next room reward** around the current game's exit-door flow. Hades II chooses rewards separately for every offered door; the trainer now keeps the override armed for all eligible exit doors and consumes it only when `StartRoom` confirms that the player entered the next room.
+- If exit rewards already exist when the user selects an override, all ordinary offered doors are patched immediately, `CurrentRun.CurrentRoom.OfferedRewards` is synchronized, and existing preview objects are refreshed in-place rather than duplicated.
+- Boss/story/shop/devotion/empty/no-reward paths remain native and are not replaced.
+
+## Compatibility
+
+Host protocol remains v5 and Hades module protocol remains v4. Lua revision is 21 because next-room runtime hook behavior changed.
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2Model.swift /mnt/data/MacGamingTrainer_v0.17.10/Sources/Hades2/Hades2Model.swift
--- /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2Model.swift	2026-09-17 16:17:31.781530230 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/Sources/Hades2/Hades2Model.swift	2026-09-17 16:45:11.472694034 +0000
@@ -32,6 +32,12 @@
     var protocolCompatible: Bool { backendStatus.protocolCompatible }
     var busy: Bool { backendStatus.busy }
     var operation: String { backendStatus.operation }
+    /// Save-management UI should remain usable while the staged-restore watcher
+    /// performs its passive process scan. All actual save operations still own
+    /// the normal busy state.
+    var saveManagerBusy: Bool {
+        busy && operation != "等待游戏退出并恢复存档"
+    }
     var error: String {
         get { backendStatus.error }
         set { backendStatus.error = newValue }
@@ -133,7 +139,6 @@
     @Published private var activationGraceFeatures: Set<String> = []
     private var hotkeys: GlobalHotkeys?
     private var pendingRestoreTimer: Timer?
-    private var featureStateTimer: Timer?
     private var shuttingDown = false
     var canSetFeature: Bool { connected && capabilities["setFeature"] == true && !exiting }
     var canEditDesired: Bool { backendAvailable && !exiting }
@@ -235,8 +240,6 @@
         invalidatePendingMutations()
         pendingRestoreTimer?.invalidate()
         pendingRestoreTimer = nil
-        featureStateTimer?.invalidate()
-        featureStateTimer = nil
         pendingRestoreID = nil
         pid = nil
         runtimeIssue = ""
@@ -364,7 +367,6 @@
         if let value = patch.elements { elements = value }
         if let value = patch.resources { resources = value }
         if let issue = patch.error, !issue.isEmpty { error = issue }
-        updateFeatureStatePolling()
     }
 
     private func applyStat(_ snapshot: Hades2StatSnapshot?, value: inout Double?, locked: inout Bool) {
@@ -433,23 +435,10 @@
         activationGraceFeatures.remove(key)
     }
 
-    /// Runtime hooks can activate/deactivate as the game moves between scenes
-    /// without a GUI command. Poll only while at least one desired feature is
-    /// enabled so presentation state (waiting/orange -> active/purple) follows
-    /// the resident Lua runtime without requiring another manual toggle.
-    private func updateFeatureStatePolling() {
-        let shouldPoll = connected && (nextRoomReward != nil || desiredFeatureKeys.contains(where: desiredFeatureEnabled))
-        if !shouldPoll {
-            featureStateTimer?.invalidate()
-            featureStateTimer = nil
-            return
-        }
-        guard featureStateTimer == nil else { return }
-        featureStateTimer = Timer.scheduledTimer(withTimeInterval: 4.0, repeats: true) { [weak self] _ in
-            guard let self, self.connected, self.backendAvailable, !self.busy, !self.exiting else { return }
-            self.send(.status, title: "同步功能状态", announceSuccess: false)
-        }
-    }
+    // Do not poll in-process Lua state automatically. Every status request crosses
+    // the LLDB/Lua boundary and briefly stops the game; periodic polling therefore
+    // creates visible frame-time spikes. Runtime state is refreshed by user-driven
+    // requests (and the explicit Refresh action) instead.
 
     func enqueueMutation(key: String, request: Hades2Request, title: String, delay: TimeInterval = 0.35) {
         mutationScheduler.schedule(key: key, delay: delay) { [weak self] in
@@ -775,8 +764,6 @@
     private func finishExit(completion: @escaping (Bool) -> Void) {
         pendingRestoreTimer?.invalidate()
         pendingRestoreTimer = nil
-        featureStateTimer?.invalidate()
-        featureStateTimer = nil
         pendingRestoreID = nil
         invalidatePendingMutations()
         shuttingDown = true
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Views/Hades2ManagementViews.swift /mnt/data/MacGamingTrainer_v0.17.10/Sources/Hades2/Views/Hades2ManagementViews.swift
--- /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Views/Hades2ManagementViews.swift	2026-09-17 14:42:25.094789129 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/Sources/Hades2/Views/Hades2ManagementViews.swift	2026-09-17 16:45:11.472876757 +0000
@@ -13,9 +13,9 @@
             HStack {
                 Label("存档管理", systemImage: "externaldrive.fill").font(.title2.bold())
                 Spacer()
-                if model.busy { ProgressView().controlSize(.small) }
-                Button("打开备份目录") { model.openBackupFolder() }.disabled(model.busy || !model.backendAvailable)
-                Button("刷新") { model.refreshBackups() }.disabled(model.busy)
+                if model.saveManagerBusy { ProgressView().controlSize(.small) }
+                Button("打开备份目录") { model.openBackupFolder() }.disabled(model.saveManagerBusy || !model.backendAvailable)
+                Button("刷新") { model.refreshBackups() }.disabled(model.saveManagerBusy)
             }
             Text("创建备份可在游戏运行时进行。运行中选择恢复会暂存任务；退出 Hades II 后 Trainer 会自动执行校验恢复，并在恢复前创建安全备份。")
                 .foregroundStyle(.secondary).fixedSize(horizontal: false, vertical: true)
@@ -47,7 +47,7 @@
                     ForEach(model.backups) { backup in
                         TrainerSelectableListRow(
                             selected: selectedBackup == backup.id,
-                            enabled: !model.busy,
+                            enabled: !model.saveManagerBusy,
                             action: { selectedBackup = backup.id }
                         ) {
                             HStack(alignment: .top, spacing: 12) {
@@ -79,28 +79,28 @@
             HStack(spacing: 10) {
                 TextField("备份名称", text: $backupRename)
                     .textFieldStyle(.roundedBorder)
-                    .disabled(selectedBackup.isEmpty || model.busy)
+                    .disabled(selectedBackup.isEmpty || model.saveManagerBusy)
                     .frame(maxWidth: .infinity)
                 Button("重命名") { model.renameBackup(selectedBackup, name: backupRename) }
-                    .disabled(model.busy || selectedBackup.isEmpty || backupRename.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
+                    .disabled(model.saveManagerBusy || selectedBackup.isEmpty || backupRename.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                 Button("在文件夹中显示") { model.openBackupFolder(selectedBackup) }
-                    .disabled(model.busy || selectedBackup.isEmpty)
+                    .disabled(model.saveManagerBusy || selectedBackup.isEmpty)
                 Button(role: .destructive) { confirmDelete = true } label: { Label("删除", systemImage: "trash") }
-                    .disabled(model.busy || selectedBackup.isEmpty)
+                    .disabled(model.saveManagerBusy || selectedBackup.isEmpty)
             }
 
             HStack {
-                Button("创建备份") { model.createBackup() }.disabled(model.busy || !model.backendAvailable)
+                Button("创建备份") { model.createBackup() }.disabled(model.saveManagerBusy || !model.backendAvailable)
                 Spacer()
-                Button("完成") { model.saveManagerPresented = false }.disabled(model.busy)
+                Button("完成") { model.saveManagerPresented = false }.disabled(model.saveManagerBusy)
                 Button("恢复所选") { confirmRestore = true }
                     .buttonStyle(.borderedProminent)
-                    .disabled(model.busy || !model.backendAvailable || !model.backups.contains(where: { $0.id == selectedBackup && $0.valid }))
+                    .disabled(model.saveManagerBusy || !model.backendAvailable || !model.backups.contains(where: { $0.id == selectedBackup && $0.valid }))
             }
         }
         .padding(28)
         .frame(width: 720)
-        .interactiveDismissDisabled(model.busy)
+        .interactiveDismissDisabled(model.saveManagerBusy)
         .alert("恢复所选存档？", isPresented: $confirmRestore) {
             Button("取消", role: .cancel) { }
             Button("备份当前并恢复") { model.restoreBackup(selectedBackup) }
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/VALIDATION_v0.17.10.txt /mnt/data/MacGamingTrainer_v0.17.10/VALIDATION_v0.17.10.txt
--- /mnt/data/MacGamingTrainer_v0.17.9/VALIDATION_v0.17.10.txt	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/VALIDATION_v0.17.10.txt	2026-09-17 16:53:15.877803111 +0000
@@ -0,0 +1,96 @@
+=== python3 -m py_compile Backend/core/__init__.py Backend/core/adapter.py Backend/core/game_spec.py Backend/core/module_manifest.py Backend/core/protocol.py Backend/core/registry.py Backend/core/server.py Backend/games/__init__.py Backend/games/hades2/__init__.py Backend/games/hades2/adapter.py Backend/games/hades2/catalog.py Backend/games/hades2/command_router.py Backend/games/hades2/config.py Backend/games/hades2/diagnostics.py Backend/games/hades2/localization.py Backend/games/hades2/preferences.py Backend/games/hades2/preparation.py Backend/games/hades2/profile_service.py Backend/games/hades2/save_service.py Backend/games/hades2/schema.py Backend/games/hades2/transport.py Tools/generate_game_binding.py Tools/module_support.py Tools/validate_game_module.py tests/legacy/test_round11_static.py tests/legacy/test_server_round10.py tests/legacy/test_server_round9.py tests/legacy/test_swift_round10.py tests/legacy/test_swift_round9.py tests/run_lua_mock.py tests/test_backend_core_round12.py tests/test_build_contract_round14.py tests/test_global_decoupling_round15.py tests/test_hades2_adapter_round12.py tests/test_hades2_mutation_scheduler_round17.py tests/test_hades2_services_round16.py tests/test_hades2_timeout_policy_v0177.py tests/test_hades2_visual_baseline_v0174.py tests/test_module_contract_round13.py tests/test_packaged_module_round14.py tests/test_preparation_round9.py tests/test_round12_static.py tests/test_round17_1_swift_scope.py tests/test_round17_static.py tests/test_runtime_reliability_round17.py tests/test_swift_declaration_uniqueness_v0176.py tests/test_swift_file_boundaries_round14_1.py tests/test_swift_integrity_v0172.py tests/test_swift_round12.py tests/test_swift_symbol_integrity_v0173.py tests/test_toggle_unification_v0178.py tests/test_ui_component_boundary_v0175.py tests/test_v01710_regressions.py tests/test_v0179_regressions.py ===
+=== python3 -c import ast; from pathlib import Path; fs=list(Path("Backend").rglob("*.py"))+list(Path("Tools").rglob("*.py"))+list(Path("tests").rglob("*.py")); [ast.parse(p.read_text(), filename=str(p), feature_version=(3,9)) for p in fs]; print("python39_grammar_ok", len(fs)) ===
+python39_grammar_ok 54
+=== python3 tests/legacy/test_round11_static.py ===
+round11_compat_ok
+=== python3 tests/legacy/test_server_round10.py ===
+server_round10_compat_ok
+=== python3 tests/legacy/test_server_round9.py ===
+server_round9_compat_ok
+=== python3 tests/legacy/test_swift_round10.py ===
+swift_round10_compat_ok
+=== python3 tests/legacy/test_swift_round9.py ===
+swift_round9_compat_ok
+=== python3 tests/test_backend_core_round12.py ===
+backend_core_round12_ok
+=== python3 tests/test_build_contract_round14.py ===
+build_contract_round14_ok
+=== python3 tests/test_global_decoupling_round15.py ===
+global_decoupling_round15_ok
+=== python3 tests/test_hades2_adapter_round12.py ===
+hades2_adapter_round12_ok
+=== python3 tests/test_hades2_mutation_scheduler_round17.py ===
+hades2_mutation_scheduler_round17_ok
+=== python3 tests/test_hades2_services_round16.py ===
+hades2_services_round16_ok
+=== python3 tests/test_hades2_timeout_policy_v0177.py ===
+hades2_timeout_policy_v0177_ok
+=== python3 tests/test_hades2_visual_baseline_v0174.py ===
+hades2_visual_baseline_v0175_ok
+=== python3 tests/test_module_contract_round13.py ===
+module_contract_round13_ok
+=== python3 tests/test_packaged_module_round14.py ===
+packaged_module_round14_ok
+=== python3 tests/test_preparation_round9.py ===
+preparation_round8_ok
+preparation_round9_staged_ok
+=== python3 tests/test_round12_static.py ===
+round12_static_ok
+=== python3 tests/test_round17_1_swift_scope.py ===
+round17_1_swift_scope_ok
+=== python3 tests/test_round17_static.py ===
+round17_static_ok
+=== python3 tests/test_runtime_reliability_round17.py ===
+runtime_reliability_round17_ok
+=== python3 tests/test_swift_declaration_uniqueness_v0176.py ===
+swift_declaration_uniqueness_v0176_ok
+=== python3 tests/test_swift_file_boundaries_round14_1.py ===
+swift_file_boundaries_round14_1_ok
+=== python3 tests/test_swift_integrity_v0172.py ===
+swift_integrity_v0172_ok
+=== python3 tests/test_swift_round12.py ===
+swift_round12_ok
+=== python3 tests/test_swift_symbol_integrity_v0173.py ===
+swift_symbol_integrity_v0173_ok
+=== python3 tests/test_toggle_unification_v0178.py ===
+toggle_unification_v0178_ok
+=== python3 tests/test_ui_component_boundary_v0175.py ===
+ui_component_boundary_v0175_ok
+=== python3 tests/test_v0179_regressions.py ===
+v0179_regressions_ok
+=== python3 tests/test_v01710_regressions.py ===
+v01710_regressions_ok
+=== python3 tests/run_lua_mock.py tests/round9_mock.lua ===
+round9_mock_ok
+=== python3 tests/run_lua_mock.py tests/round10_mock.lua ===
+round10_mock_ok
+=== bash -n build.sh ===
+=== python3 Tools/validate_game_module.py hades2 ===
+hades2: ok
+=== python3 Tools/generate_game_binding.py hades2 /tmp/tmp.hB4DFjEuL1/ActiveGame.generated.swift ===
+=== swiftc -frontend -parse Sources/App.swift Sources/Core/**/*.swift Sources/Hades2/**/*.swift ActiveGame.generated.swift ===
+=== python3 -c 'import plistlib; p=plistlib.load(open("Info.plist","rb")); assert p["CFBundleShortVersionString"]=="0.17.10"; assert p["CFBundleVersion"]=="33"; print("plist_version_ok")' ===
+plist_version_ok
+=== python3 -c 'from pathlib import Path; s=Path("Backend/games/hades2/runtime/hades.lua").read_text(); assert "revision = 21" in s; assert "ForceLootName = \"SpellDrop\"" in s; assert "type(args.Door) ~= \"table\"" in s; print("lua_revision_reward_flow_ok")' ===
+lua_revision_reward_flow_ok
+=== python3 -c 'from pathlib import Path; s=Path("Sources/Hades2/Hades2Model.swift").read_text(); assert "featureStateTimer" not in s and "同步功能状态" not in s and "saveManagerBusy" in s; print("no_live_status_polling_ok")' ===
+no_live_status_polling_ok
+=== python3 -c 'from pathlib import Path; s=Path("Sources/Hades2/Views/Hades2ManagementViews.swift").read_text().split("struct Hades2ProfileManagerView")[0]; assert "model.busy" not in s and "model.saveManagerBusy" in s; print("save_manager_passive_scan_ok")' ===
+save_manager_passive_scan_ok
+=== python3 -c 'from pathlib import Path; s=Path("Sources/Core/UI/Primitives/TrainerToggleControl.swift").read_text(); assert ".controlSize(.regular)" in s and ".tint(theme.accent)" in s; print("shared_switch_ok")' ===
+shared_switch_ok
+=== python3 -c 'from pathlib import Path; s="\\n".join(p.read_text() for p in Path("Sources/Hades2").rglob("*.swift")); assert "Toggle(" not in s; print("hades_no_direct_toggle_ok")' ===
+hades_no_direct_toggle_ok
+
+NOTE: Linux validation cannot perform final macOS SwiftUI/AppKit semantic typecheck/link, codesign, LLDB Python import, or real Hades II attach. build.sh performs target-Mac semantic typecheck before optimized compilation.
+
+VALIDATION_v0.17.10_OK
+
+=== post-clean release sanity ===
+python3 tests/test_v01710_regressions.py -> v01710_regressions_ok
+python3 tests/test_v0179_regressions.py -> v0179_regressions_ok
+python3 tests/run_lua_mock.py tests/round9_mock.lua -> round9_mock_ok
+python3 tests/run_lua_mock.py tests/round10_mock.lua -> round10_mock_ok
+python3 Tools/validate_game_module.py hades2 -> hades2: ok
+bash -n build.sh -> ok
+POST_CLEAN_SANITY_OK
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/legacy/test_round11_static.py /mnt/data/MacGamingTrainer_v0.17.10/tests/legacy/test_round11_static.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/legacy/test_round11_static.py	2026-09-17 16:15:52.360136024 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/legacy/test_round11_static.py	2026-09-17 16:46:18.893758964 +0000
@@ -4,7 +4,7 @@
 lua = (root/'Backend/games/hades2/runtime/hades.lua').read_text()
 model = (root/'Sources/Hades2/Hades2Model.swift').read_text()
 presentation = (root/'Sources/Hades2/Presentation/Hades2FeaturePresentation.swift').read_text()
-assert 'revision = 20' in lua
+assert 'revision = 21' in lua
 assert 'ActiveProjectileCap = 32' in lua
 assert 'type(CurrentRun.ResourcesSpent) == "table"' in lua
 assert 'statAvailable = statAvailable' in lua
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/round10_mock.lua /mnt/data/MacGamingTrainer_v0.17.10/tests/round10_mock.lua
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/round10_mock.lua	2026-09-17 16:16:17.517734033 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/round10_mock.lua	2026-09-17 16:47:43.856742453 +0000
@@ -19,9 +19,18 @@
   SpellDrop = { RequiredPackage = "SpellPackage", SetupEvents = { { FunctionName = "PregenerateSpells" } } },
 }
 RewardData = { WeaponUpgrade={}, StackUpgrade={}, Boon={} }
+StartRoom = function(run,room)
+  if type(run)=='table' then run.CurrentRoom=room end
+  return room
+end
 ChooseRoomReward = function(run,room,rewardStoreName,previouslyChosen,args)
   room.Reward={Name="RoomMoneyDrop"}; return "RoomMoneyDrop"
 end
+nextRoomPreviewRefreshes=0
+CreateDoorRewardPreview = function(door, rewardType, forceLoot, index, args)
+  if args and args.ReUseIds then nextRoomPreviewRefreshes=nextRoomPreviewRefreshes+1 end
+end
+RefreshUseButton = function(id,door) end
 ScreenData = { UpgradeChoice = { MaxChoices = 3 } }
 CalcNumLootChoices = function(lootData) return ScreenData.UpgradeChoice.MaxChoices end
 GetTotalLootChoices = function() return ScreenData.UpgradeChoice.MaxChoices end
@@ -691,21 +700,48 @@
 state=M.dispatch('set_next_room_reward',{reward='WeaponUpgrade'})
 assert(state.nextRoomReward=='WeaponUpgrade' and state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward hook did not arm')
 local forcedRoom={Encounter={Name='Encounter'}}
-local chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
+local chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{Door={ObjectId=9001}})
 assert(chosen=='WeaponUpgrade' and forcedRoom.Reward.Name=='WeaponUpgrade','next-room ordinary reward override failed')
 state=M.dispatch('status',{})
-assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was not consumed/released')
+assert(state.nextRoomReward=='WeaponUpgrade' and state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was consumed before the player entered a door')
+local enteredRoom=forcedRoom
+StartRoom(CurrentRun,enteredRoom)
+state=M.dispatch('status',{})
+assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was not consumed after entering the selected room')
 state=M.dispatch('set_next_room_reward',{reward='ZeusUpgrade'})
 forcedRoom={Encounter={Name='Encounter'}}
-chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
+chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{Door={ObjectId=9001}})
 assert(chosen=='Boon' and forcedRoom.ForceLootName=='ZeusUpgrade','next-room god reward override failed')
 M.dispatch('set_next_room_reward',{reward=nil})
 M.dispatch('set_next_room_reward',{reward='MetaCurrencyDrop'})
 forcedRoom={Encounter={Name='Encounter'}}
-chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
+chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{Door={ObjectId=9001}})
 assert(chosen=='MetaCurrencyDrop' and forcedRoom.Reward.Name=='MetaCurrencyDrop','next-room meta currency reward override failed')
 M.dispatch('set_next_room_reward',{reward=nil})
 
+-- If exit rewards were already materialized before the trainer command,
+-- every ordinary offered door must be patched immediately and the one-shot
+-- still remains armed until the selected room is actually entered.
+local originRoom=CurrentRun.CurrentRoom
+local offeredA={ChosenRewardType='RoomMoneyDrop',Reward={Name='RoomMoneyDrop'},Encounter={Name='Encounter'}}
+local offeredB={ChosenRewardType='MaxManaDrop',Reward={Name='MaxManaDrop'},Encounter={Name='Encounter'}}
+MapState.OfferedExitDoors={
+  [9101]={ObjectId=9101,Room=offeredA,RewardPreviewIconIds={101}},
+  [9102]={ObjectId=9102,Room=offeredB,RewardPreviewIconIds={102}},
+}
+CurrentRun.CurrentRoom=originRoom
+CurrentRun.CurrentRoom.OfferedRewards={}
+nextRoomPreviewRefreshes=0
+state=M.dispatch('set_next_room_reward',{reward='MaxHealthDrop'})
+assert(offeredA.ChosenRewardType=='MaxHealthDrop' and offeredB.ChosenRewardType=='MaxHealthDrop','existing offered doors were not all patched')
+assert(CurrentRun.CurrentRoom.OfferedRewards[9101].Type=='MaxHealthDrop' and CurrentRun.CurrentRoom.OfferedRewards[9102].Type=='MaxHealthDrop','offered reward cache was not synchronized')
+assert(state.nextRoomReward=='MaxHealthDrop' and state.runtimeDiagnostics.nextRoomRewardPatchedDoors==2,'existing door patch diagnostics are wrong')
+assert(nextRoomPreviewRefreshes==2,'existing reward previews were not refreshed in place')
+StartRoom(CurrentRun,offeredB)
+state=M.dispatch('status',{})
+assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'patched next-room reward did not consume on room entry')
+MapState.OfferedExitDoors={}
+
 -- Garden QoL uses the game's own multi-plant and harvest-all paths without
 -- permanently unlocking the WorldUpgradeGardenHarvestAll upgrade.
 state=setFeature('gardenQoL',true)
@@ -809,11 +845,11 @@
 -- so the native SpawnRoomReward path calls GiveLoot.
 state=M.dispatch('set_next_room_reward',{reward='MaxHealthDrop'})
 local overrideRoom={Encounter={Name='Combat'}}
-local chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{})
+local chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{Door={ObjectId=9002}})
 assert(chosen=='MaxHealthDrop' and overrideRoom.ChosenRewardType=='MaxHealthDrop' and overrideRoom.Reward.Name=='MaxHealthDrop','current direct next-room override failed')
 state=M.dispatch('set_next_room_reward',{reward='SpellDrop'})
 overrideRoom={Encounter={Name='Combat'}}
-chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{})
+chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{Door={ObjectId=9002}})
 assert(chosen=='Boon' and overrideRoom.ChosenRewardType=='Boon' and overrideRoom.ForceLootName=='SpellDrop' and overrideRoom.Reward.ForceLootName=='SpellDrop','Selene next-room override did not use native Boon path')
 
 -- Spawn each supported kind without invoking NPC narrative state.
@@ -887,7 +923,7 @@
 assert(state.activeFeatures.gameSpeed and math.abs(_elapsedTimeMultiplier-1.25)<1e-9,'session-scoped game speed did not survive crossroads-to-run transition')
 assert(state.nextRoomReward=='WeaponUpgrade' and state.runtimeDiagnostics.nextRoomRewardHook,'pending next-room reward was lost on crossroads-to-run transition')
 local crossroadsForcedRoom={Encounter={Name='Encounter'}}
-assert(ChooseRoomReward(CurrentRun,crossroadsForcedRoom,'RunProgress',nil,{})=='WeaponUpgrade' and crossroadsForcedRoom.Reward.Name=='WeaponUpgrade','crossroads next-room reward did not survive into the run')
+assert(ChooseRoomReward(CurrentRun,crossroadsForcedRoom,'RunProgress',nil,{Door={ObjectId=9003}})=='WeaponUpgrade' and crossroadsForcedRoom.Reward.Name=='WeaponUpgrade','crossroads next-room reward did not survive into the run')
 M.dispatch('disable_all',{})
 assert(GetMaxMetaUpgradeCost()==30 and math.abs(_elapsedTimeMultiplier-1)<1e-9,'crossroads global state leaked after disable_all')
 print('round10_mock_ok')
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/round9_mock.lua /mnt/data/MacGamingTrainer_v0.17.10/tests/round9_mock.lua
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/round9_mock.lua	2026-09-17 16:20:12.845830008 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/round9_mock.lua	2026-09-17 16:47:43.856455493 +0000
@@ -18,9 +18,18 @@
   SpellDrop = { RequiredPackage = "SpellPackage", SetupEvents = { { FunctionName = "PregenerateSpells" } } },
 }
 RewardData = { WeaponUpgrade={}, StackUpgrade={}, Boon={} }
+StartRoom = function(run,room)
+  if type(run)=='table' then run.CurrentRoom=room end
+  return room
+end
 ChooseRoomReward = function(run,room,rewardStoreName,previouslyChosen,args)
   room.Reward={Name="RoomMoneyDrop"}; return "RoomMoneyDrop"
 end
+nextRoomPreviewRefreshes=0
+CreateDoorRewardPreview = function(door, rewardType, forceLoot, index, args)
+  if args and args.ReUseIds then nextRoomPreviewRefreshes=nextRoomPreviewRefreshes+1 end
+end
+RefreshUseButton = function(id,door) end
 TraitData = {
   SupportingFireBoon = {}, InsideCastCritBoon = {}, EchoLastRewardBoon = {}, SpellMockTrait = {},
   NormalRarityBoon = {}, LegendaryMockBoon = { InheritFrom = { "LegendaryTrait" } }, DuoMockBoon = { InheritFrom = { "SynergyTrait" } },
@@ -658,16 +667,43 @@
 state=M.dispatch('set_next_room_reward',{reward='WeaponUpgrade'})
 assert(state.nextRoomReward=='WeaponUpgrade' and state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward hook did not arm')
 local forcedRoom={Encounter={Name='Encounter'}}
-local chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
+local chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{Door={ObjectId=9001}})
 assert(chosen=='WeaponUpgrade' and forcedRoom.Reward.Name=='WeaponUpgrade','next-room ordinary reward override failed')
 state=M.dispatch('status',{})
-assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was not consumed/released')
+assert(state.nextRoomReward=='WeaponUpgrade' and state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was consumed before the player entered a door')
+local enteredRoom=forcedRoom
+StartRoom(CurrentRun,enteredRoom)
+state=M.dispatch('status',{})
+assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'next-room reward was not consumed after entering the selected room')
 state=M.dispatch('set_next_room_reward',{reward='ZeusUpgrade'})
 forcedRoom={Encounter={Name='Encounter'}}
-chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
+chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{Door={ObjectId=9001}})
 assert(chosen=='Boon' and forcedRoom.ForceLootName=='ZeusUpgrade','next-room god reward override failed')
 M.dispatch('set_next_room_reward',{reward=nil})
 
+-- If exit rewards were already materialized before the trainer command,
+-- every ordinary offered door must be patched immediately and the one-shot
+-- still remains armed until the selected room is actually entered.
+local originRoom=CurrentRun.CurrentRoom
+local offeredA={ChosenRewardType='RoomMoneyDrop',Reward={Name='RoomMoneyDrop'},Encounter={Name='Encounter'}}
+local offeredB={ChosenRewardType='MaxManaDrop',Reward={Name='MaxManaDrop'},Encounter={Name='Encounter'}}
+MapState.OfferedExitDoors={
+  [9101]={ObjectId=9101,Room=offeredA,RewardPreviewIconIds={101}},
+  [9102]={ObjectId=9102,Room=offeredB,RewardPreviewIconIds={102}},
+}
+CurrentRun.CurrentRoom=originRoom
+CurrentRun.CurrentRoom.OfferedRewards={}
+nextRoomPreviewRefreshes=0
+state=M.dispatch('set_next_room_reward',{reward='MaxHealthDrop'})
+assert(offeredA.ChosenRewardType=='MaxHealthDrop' and offeredB.ChosenRewardType=='MaxHealthDrop','existing offered doors were not all patched')
+assert(CurrentRun.CurrentRoom.OfferedRewards[9101].Type=='MaxHealthDrop' and CurrentRun.CurrentRoom.OfferedRewards[9102].Type=='MaxHealthDrop','offered reward cache was not synchronized')
+assert(state.nextRoomReward=='MaxHealthDrop' and state.runtimeDiagnostics.nextRoomRewardPatchedDoors==2,'existing door patch diagnostics are wrong')
+assert(nextRoomPreviewRefreshes==2,'existing reward previews were not refreshed in place')
+StartRoom(CurrentRun,offeredB)
+state=M.dispatch('status',{})
+assert(state.nextRoomReward==nil and not state.runtimeDiagnostics.nextRoomRewardHook,'patched next-room reward did not consume on room entry')
+MapState.OfferedExitDoors={}
+
 -- Garden QoL uses the game's own multi-plant and harvest-all paths without
 -- permanently unlocking the WorldUpgradeGardenHarvestAll upgrade.
 state=setFeature('gardenQoL',true)
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/test_backend_core_round12.py /mnt/data/MacGamingTrainer_v0.17.10/tests/test_backend_core_round12.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/test_backend_core_round12.py	2026-09-17 16:18:39.508039585 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/test_backend_core_round12.py	2026-09-17 16:45:11.474054740 +0000
@@ -34,7 +34,7 @@
 
 
 assert HOST_PROTOCOL_VERSION == PROTOCOL_VERSION == 5
-assert APP_BACKEND_VERSION == '0.17.9'
+assert APP_BACKEND_VERSION == '0.17.10'
 assert any(row['id'] == 'hades2' and row['protocolVersion'] == 4 for row in available_games())
 
 context = GameAdapterContext(
@@ -46,7 +46,7 @@
 hello = router.handle({'id':'hello','command':'hello','params':{}})
 assert hello['ok'] and hello['protocolVersion'] == 5
 assert hello['gameID'] == 'fake' and hello['moduleProtocolVersion'] == 7
-assert hello['result']['backendVersion'] == '0.17.9'
+assert hello['result']['backendVersion'] == '0.17.10'
 reply = router.handle({'id':'a','command':'poke','params':{'x':1}})
 assert reply['ok'] and reply['result']['command'] == 'poke'
 assert router.handle({'id':'a','command':'poke','params':{'x':1}}) == reply
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round12_static.py /mnt/data/MacGamingTrainer_v0.17.10/tests/test_round12_static.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round12_static.py	2026-09-17 16:15:52.360054602 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/test_round12_static.py	2026-09-17 16:46:18.893621368 +0000
@@ -13,7 +13,7 @@
 manifest = json.loads((root/'Backend/games/hades2/module.json').read_text())
 
 # Hades gameplay behavior retained.
-for token in ('revision = 20','ActiveProjectileCap = 32','nativeMultiCastControlSet','type(CurrentRun.ResourcesSpent) == "table"','statAvailable = statAvailable'):
+for token in ('revision = 21','ActiveProjectileCap = 32','nativeMultiCastControlSet','type(CurrentRun.ResourcesSpent) == "table"','statAvailable = statAvailable'):
     assert token in lua, token
 
 # Game-internal responsibilities are split instead of teaching Core Hades concepts.
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round17_static.py /mnt/data/MacGamingTrainer_v0.17.10/tests/test_round17_static.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round17_static.py	2026-09-17 16:18:39.508127526 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/test_round17_static.py	2026-09-17 16:45:11.474154559 +0000
@@ -45,7 +45,7 @@
 
 with (ROOT / 'Info.plist').open('rb') as handle:
     plist = plistlib.load(handle)
-assert plist['CFBundleShortVersionString'] == '0.17.9'
-assert plist['CFBundleVersion'] == '32'
+assert plist['CFBundleShortVersionString'] == '0.17.10'
+assert plist['CFBundleVersion'] == '33'
 
 print('round17_static_ok')
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v01710_regressions.py /mnt/data/MacGamingTrainer_v0.17.10/tests/test_v01710_regressions.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v01710_regressions.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/test_v01710_regressions.py	2026-09-17 16:45:11.474810621 +0000
@@ -0,0 +1,32 @@
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[1]
+model = (ROOT/'Sources/Hades2/Hades2Model.swift').read_text()
+management = (ROOT/'Sources/Hades2/Views/Hades2ManagementViews.swift').read_text()
+lua = (ROOT/'Backend/games/hades2/runtime/hades.lua').read_text()
+
+# The v0.17.9 4-second live status timer caused debugger boundary stops in play.
+assert 'featureStateTimer' not in model
+assert 'updateFeatureStatePolling' not in model
+assert '同步功能状态' not in model
+
+# Passive staged-restore scans do not lock/flash the Save Manager itself.
+assert 'var saveManagerBusy: Bool' in model
+save_view = management[:management.index('struct Hades2ProfileManagerView')]
+assert 'model.saveManagerBusy' in save_view
+assert 'model.busy' not in save_view
+
+# Next-room reward is constrained to real exit-door reward selection, persists
+# across every offered door, and is consumed only when the player enters a room.
+assert 'requireFunctions("next room reward", { "ChooseRoomReward", "StartRoom" })' in lua
+assert 'type(args.Door) ~= "table"' in lua
+assert 'M.nextRoomReward = nil\n            releaseNextRoomReward()' in lua
+choose_block = lua[lua.index('installHook("ChooseRoomReward"'):lua.index('if not owns("StartRoom")')]
+assert 'M.nextRoomReward = nil' not in choose_block
+assert 'patchOfferedNextRoomDoors()' in lua
+assert 'CurrentRun.CurrentRoom.OfferedRewards[doorId]' in lua
+assert 'ReUseIds = true' in lua
+assert 'nextRoomRewardPatchedDoors' in lua
+assert 'revision = 21' in lua
+
+print('v01710_regressions_ok')
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=.pytest_cache' '--exclude=CHANGES_v0.17.9_to_v0.17.10.diff' /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v0179_regressions.py /mnt/data/MacGamingTrainer_v0.17.10/tests/test_v0179_regressions.py
--- /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v0179_regressions.py	2026-09-17 16:18:08.261717812 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.10/tests/test_v0179_regressions.py	2026-09-17 16:47:06.640213122 +0000
@@ -39,9 +39,11 @@
 assert 'model.busy' not in save_line
 assert 'model.exiting' in save_line
 
-# Runtime-state polling reconciles autonomous scene activation/deactivation.
+# Never poll live Lua automatically: every status call crosses the debugger
+# boundary and can stop the game for hundreds of milliseconds.
 for token in ('featureStateTimer', 'updateFeatureStatePolling()', 'withTimeInterval: 4.0', 'self.send(.status, title: "同步功能状态", announceSuccess: false)'):
-    assert token in model, token
+    assert token not in model, token
+assert 'saveManagerBusy' in model
 
 # Next-room UI and Lua both use current reward IDs, not early-access RoomReward* IDs.
 for token in ('RoomMoneyDrop','MetaCurrencyDrop','MetaCardPointsCommonDrop','MemPointsCommonDrop','MaxHealthDrop','MaxManaDrop','StackUpgrade','WeaponUpgrade','SpellDrop'):
@@ -49,13 +51,13 @@
 for old in ('RoomRewardMoney','RoomRewardMetaPoint','RoomRewardPsyche','RoomRewardMixerFabric','RoomRewardMaxHealth','RoomRewardPom'):
     assert old not in view, old
 assert 'room.ChosenRewardType = rewardType' in lua
-assert 'rewardType, forceLoot = "Boon", wanted' in lua
+assert 'return "Boon", wanted' in lua
 
 # SpellDrop is the special case that must use the game's native GiveLoot path.
 assert 'if rewardId == "SpellDrop" then' in lua
 assert 'requireFunctions("Selene loot spawning", { "GiveLoot" })' in lua
 assert 'GiveLoot({ ForceLootName = "SpellDrop"' in lua
-assert 'revision = 20' in lua
+assert 'revision = 21' in lua
 
 # Old stored preferences migrate forward instead of silently retaining dead IDs.
 normalized = Hades2PreferenceStore.normalize({'nextRoomReward':'RoomRewardMoney'})
