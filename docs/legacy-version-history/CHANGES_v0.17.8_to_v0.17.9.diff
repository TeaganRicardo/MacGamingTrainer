diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/BUILDING.md /mnt/data/MacGamingTrainer_v0.17.9/BUILDING.md
--- /mnt/data/MacGamingTrainer_v0.17.8/BUILDING.md	2026-09-17 15:52:44.381746197 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/BUILDING.md	2026-09-17 16:18:39.508236359 +0000
@@ -1,4 +1,4 @@
-# Build v0.17.8
+# Build v0.17.9
 
 当前唯一支持的构建方式是手动运行 `build.sh`。此前实验性的 Finder 双击构建 App 已暂时移除，因为在下载目录中会受到 macOS Finder 权限与 bundle 路径解析影响。
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Backend/core/protocol.py /mnt/data/MacGamingTrainer_v0.17.9/Backend/core/protocol.py
--- /mnt/data/MacGamingTrainer_v0.17.8/Backend/core/protocol.py	2026-09-17 15:21:17.315405526 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Backend/core/protocol.py	2026-09-17 16:18:39.507976871 +0000
@@ -6,7 +6,7 @@
 # module has its own independent protocol/schema version exposed alongside it.
 HOST_PROTOCOL_VERSION = 5
 PROTOCOL_VERSION = HOST_PROTOCOL_VERSION  # compatibility alias for older module code
-APP_BACKEND_VERSION = '0.17.8'
+APP_BACKEND_VERSION = '0.17.9'
 
 
 from core.adapter import GameAdapter
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/adapter.py /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/adapter.py
--- /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/adapter.py	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/adapter.py	2026-09-17 16:16:59.093447321 +0000
@@ -355,6 +355,13 @@
             decoded['capabilities']=capabilities
             self.state.update(decoded,connected=True,pid=self.transport.pid)
             self.state.pop('error',None)
+            # nextRoomReward is a one-shot runtime request. Once Lua consumes it,
+            # a later clean status omits the field; clear the persisted desired
+            # value too so it is not visually resurrected or replayed on reconnect.
+            if command=='status' and not self.preference_dirty and self.preferences.get('nextRoomReward') is not None and decoded.get('nextRoomReward') is None:
+                self.preferences['nextRoomReward']=None
+                self.state['nextRoomReward']=None
+                self._save_preferences()
             if command in ('disable_all','cleanup'):
                 self._reset_preferences()
             elif command=='set_feature' and not replay:
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/preferences.py /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/preferences.py
--- /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/preferences.py	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/preferences.py	2026-09-17 16:14:42.991999035 +0000
@@ -5,6 +5,19 @@
 from .schema import MULTIPLIERS, TOGGLES
 
 
+NEXT_ROOM_REWARD_MIGRATIONS = {
+    # Pre-1.0 / early-access reward identifiers retained by older trainer profiles.
+    'RoomRewardMoney': 'RoomMoneyDrop',
+    'RoomRewardMetaPoint': 'MetaCardPointsCommonDrop',
+    'RoomRewardPsyche': 'MemPointsCommonDrop',
+    'RoomRewardMaxHealth': 'MaxHealthDrop',
+    'RoomRewardPom': 'StackUpgrade',
+    # There is no current room-door equivalent for the old direct fabric reward.
+    'RoomRewardMixerFabric': None,
+    'RoomRewardTrash': None,
+}
+
+
 class Hades2PreferenceStore:
     def __init__(self, path):
         self.path = path
@@ -43,7 +56,8 @@
         value=raw.get('rerollsLock')
         if type(value) in (int,float) and not isinstance(value,bool) and math.isfinite(value) and value>=0:result['rerollsLock']=value
         reward=raw.get('nextRoomReward')
-        if reward is None or isinstance(reward,str):result['nextRoomReward']=reward
+        if reward is None or isinstance(reward,str):
+            result['nextRoomReward']=NEXT_ROOM_REWARD_MIGRATIONS.get(reward,reward)
         return result
 
     def load(self):
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/runtime/hades.lua /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/runtime/hades.lua
--- /mnt/data/MacGamingTrainer_v0.17.8/Backend/games/hades2/runtime/hades.lua	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Backend/games/hades2/runtime/hades.lua	2026-09-17 16:15:15.604519501 +0000
@@ -6,13 +6,13 @@
 end
 if type(UpdateTimers) ~= "function" then error("Unsupported game runtime: missing UpdateTimers") end
 local previousModule = __MacGamingTrainerV1
-if previousModule and previousModule.revision ~= 19 then
+if previousModule and previousModule.revision ~= 20 then
   previousModule.dispatch("cleanup")
   __MacGamingTrainerV1 = nil
 end
 if __MacGamingTrainerV1 == nil then
   local M = {
-    version = 1, revision = 19, damageMultiplier = 2, damageEnabled = false,
+    version = 1, revision = 20, damageMultiplier = 2, damageEnabled = false,
     gameSpeed = 1, gameSpeedActive = false, gameSpeedCallStyle = nil,
     gameSpeedMethod = nil, gameSpeedAppliedValue = nil,
     godMode = false, infiniteHealth = false, infiniteMana = false,
@@ -2040,25 +2040,34 @@
   local function installNextRoomReward()
     requireFunctions("next room reward", { "ChooseRoomReward" })
     if owns("ChooseRoomReward") then return end
-    local safeNative = {
-      RoomRewardMoney=true, RoomRewardMetaPoint=true, RoomRewardMaxHealth=true, Boon=true,
-      RoomRewardPom=true, RoomRewardTrash=true, RoomRewardPsyche=true, RoomRewardMixerFabric=true,
-      WeaponUpgrade=true, HermesUpgrade=true, SpellDrop=true,
+    local directReward = {
+      RoomMoneyDrop=true, MetaCurrencyDrop=true, MetaCardPointsCommonDrop=true, MemPointsCommonDrop=true,
+      MaxHealthDrop=true, MaxManaDrop=true, StackUpgrade=true, WeaponUpgrade=true, HermesUpgrade=true,
     }
     installHook("ChooseRoomReward", function(original, run, room, rewardStoreName, previouslyChosenRewards, args, ...)
       local native = original(run, room, rewardStoreName, previouslyChosenRewards, args, ...)
       local wanted = M.nextRoomReward
-      if wanted == nil or type(room) ~= "table" or not safeNative[native] then return native end
+      if wanted == nil or type(room) ~= "table" or type(native) ~= "string" then return native end
       if type(room.Encounter) == "table" and type(room.Encounter.Name) == "string" and string.find(room.Encounter.Name, "Boss", 1, true) then return native end
+      local nativeKnown = native == "Boon" or directReward[native] == true
+        or (type(ConsumableData) == "table" and type(ConsumableData[native]) == "table")
+        or (type(LootData) == "table" and type(LootData[native]) == "table")
+      if not nativeKnown then return native end
+
       local rewardType, forceLoot = wanted, nil
-      local isDirect = wanted == "RoomRewardMoney" or wanted == "RoomRewardMetaPoint" or wanted == "RoomRewardPsyche" or wanted == "RoomRewardMixerFabric" or wanted == "RoomRewardMaxHealth" or wanted == "RoomRewardPom" or wanted == "WeaponUpgrade" or wanted == "SpellDrop"
-      if not isDirect and type(LootData) == "table" and type(LootData[wanted]) == "table" then
+      if not directReward[wanted] then
+        if type(LootData) ~= "table" or type(LootData[wanted]) ~= "table" then return native end
         rewardType, forceLoot = "Boon", wanted
       end
-      if type(RewardData) == "table" and RewardData[rewardType] == nil and rewardType ~= "Boon" then return native end
+
       room.Reward = { Name = rewardType }
-      if forceLoot ~= nil then room.Reward.ForceLootName = forceLoot; room.ForceLootName = forceLoot
-      else room.ForceLootName = nil end
+      room.ChosenRewardType = rewardType
+      if forceLoot ~= nil then
+        room.Reward.ForceLootName = forceLoot
+        room.ForceLootName = forceLoot
+      else
+        room.ForceLootName = nil
+      end
       M.nextRoomReward = nil
       releaseHook("ChooseRoomReward")
       return rewardType
@@ -2637,7 +2646,9 @@
       if reward ~= nil and type(reward) ~= "string" then error("Next room reward must be a string or nil") end
       if reward == "" then reward = nil end
       if reward ~= nil then
-        local validDirect = reward == "RoomRewardMoney" or reward == "RoomRewardMetaPoint" or reward == "RoomRewardPsyche" or reward == "RoomRewardMixerFabric" or reward == "RoomRewardMaxHealth" or reward == "RoomRewardPom" or reward == "WeaponUpgrade" or reward == "SpellDrop"
+        local validDirect = reward == "RoomMoneyDrop" or reward == "MetaCurrencyDrop" or reward == "MetaCardPointsCommonDrop"
+          or reward == "MemPointsCommonDrop" or reward == "MaxHealthDrop" or reward == "MaxManaDrop"
+          or reward == "StackUpgrade" or reward == "WeaponUpgrade" or reward == "HermesUpgrade"
         local validLoot = type(LootData) == "table" and type(LootData[reward]) == "table"
         if not validDirect and not validLoot then error("Unsupported next room reward") end
         M.nextRoomReward = reward
@@ -2800,6 +2811,14 @@
           end
           return objectId
         end
+        if rewardId == "SpellDrop" then
+          requireFunctions("Selene loot spawning", { "GiveLoot" })
+          local loot = GiveLoot({ ForceLootName = "SpellDrop", SpawnPoint = CurrentRun.Hero.ObjectId,
+            OffsetX = 100, AutoLoadPackages = true, DoesNotBlockExit = true })
+          if type(loot) ~= "table" or not finite(loot.ObjectId) then error("Selene loot spawn did not return an object ID") end
+          loot.DoesNotBlockExit = true
+          return loot.ObjectId
+        end
         requireFunctions("loot spawning", { "CreateLoot" })
         if type(MapState) ~= "table" or type(MapState.RoomRequiredObjects) ~= "table"
             or type(LootObjects) ~= "table" then error("Unsupported loot spawning: scene loot tables unavailable") end
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/DEFERRED_REFACTOR_PLAN.md /mnt/data/MacGamingTrainer_v0.17.9/DEFERRED_REFACTOR_PLAN.md
--- /mnt/data/MacGamingTrainer_v0.17.8/DEFERRED_REFACTOR_PLAN.md	2026-09-17 15:52:44.197744865 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/DEFERRED_REFACTOR_PLAN.md	2026-09-17 16:18:39.508411441 +0000
@@ -1,4 +1,4 @@
-# Deferred work after v0.17.8
+# Deferred work after v0.17.9
 
 The framework-level corrective refactor and the first runtime-reliability pass are complete. Do not resume structural refactoring unless a concrete requirement demonstrates a missing host capability.
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/GAME_MODULES.md /mnt/data/MacGamingTrainer_v0.17.9/GAME_MODULES.md
--- /mnt/data/MacGamingTrainer_v0.17.8/GAME_MODULES.md	2026-09-17 15:52:44.461746781 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/GAME_MODULES.md	2026-09-17 16:18:39.508492523 +0000
@@ -1,4 +1,4 @@
-# Game module contract — v0.17.8
+# Game module contract — v0.17.9
 
 A normal new game should add only:
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Info.plist /mnt/data/MacGamingTrainer_v0.17.9/Info.plist
--- /mnt/data/MacGamingTrainer_v0.17.8/Info.plist	2026-09-17 15:21:17.315346929 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Info.plist	2026-09-17 16:18:39.507838103 +0000
@@ -17,9 +17,9 @@
 	<key>CFBundlePackageType</key>
 	<string>APPL</string>
 	<key>CFBundleShortVersionString</key>
-	<string>0.17.8</string>
+	<string>0.17.9</string>
 	<key>CFBundleVersion</key>
-	<string>31</string>
+	<string>32</string>
 	<key>LSMinimumSystemVersion</key>
 	<string>14.0</string>
 	<key>NSHighResolutionCapable</key>
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/REFACTOR_ASSESSMENT.md /mnt/data/MacGamingTrainer_v0.17.9/REFACTOR_ASSESSMENT.md
--- /mnt/data/MacGamingTrainer_v0.17.8/REFACTOR_ASSESSMENT.md	2026-09-17 15:52:44.529747275 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/REFACTOR_ASSESSMENT.md	2026-09-17 16:18:39.508690399 +0000
@@ -1,4 +1,4 @@
-# v0.17.8 architecture assessment
+# v0.17.9 architecture assessment
 
 ## Current verdict
 
@@ -10,7 +10,7 @@
 
 The current implementation meets that criterion for the tested module contract.
 
-v0.17.8 tightens the UI side of those boundaries without moving Hades business semantics into Core. Reusable visual controls now live in `Sources/Core/UI`; Hades keeps only page composition and the mapping from Hades runtime semantics to game-agnostic visual state. The shared controls preserve the proven Hades visual language instead of introducing another visual system. In v0.17.8 every switch path converges on one native small purple `TrainerToggleControl`; Hades owns no switch rendering logic. Runtime protocol/game behaviour remains unchanged.
+v0.17.9 tightens the UI side of those boundaries without moving Hades business semantics into Core. Reusable visual controls now live in `Sources/Core/UI`; Hades keeps only page composition and the mapping from Hades runtime semantics to game-agnostic visual state. The shared controls preserve the proven Hades visual language instead of introducing another visual system. In v0.17.9 every switch path still converges on one shared native purple `TrainerToggleControl`; Hades owns no switch rendering logic. The switch now uses regular macOS control size. This release also restores current-game next-room reward identifiers, native Selene `GiveLoot` spawning, and lightweight live feature-state reconciliation without moving any Hades semantics into Core.
 
 ## Framework responsibilities
 
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/RELEASE_NOTES_v0.17.9.md /mnt/data/MacGamingTrainer_v0.17.9/RELEASE_NOTES_v0.17.9.md
--- /mnt/data/MacGamingTrainer_v0.17.8/RELEASE_NOTES_v0.17.9.md	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/RELEASE_NOTES_v0.17.9.md	2026-09-17 16:18:39.677888085 +0000
@@ -0,0 +1,61 @@
+# v0.17.9
+
+App version: **0.17.9**  
+Build: **32**  
+Host protocol: **v5 (unchanged)**  
+Hades II module protocol: **v4 (unchanged)**  
+Lua runtime revision: **20**
+
+## 局内数据 UI
+
+- 修正 `TrainerMetricCard` 的高度计算顺序：padding 现在包含在卡片总高度内，不再形成顶部大块空白和异常高卡片。
+- 生命/魔力 `X / Y` 输入区域收紧，`/` 保持在两个数值中央。
+- 百分比和 `/s` 后缀改为紧邻数值。
+- 卡片内容重新垂直居中。
+- 展开/收起改为 `文字 + 箭头` 的单一紧凑点击区域，箭头本身也可点击。
+
+## Switch 与状态同步
+
+- 唯一共享 `TrainerToggleControl` 从 `.small` 调整到 `.regular`，继续使用 `theme.accent` 的亮紫色和原生动画。
+- Hades2 仍然没有独立 Switch 渲染实现。
+- 增加轻量 runtime state reconciliation：只要存在已开启 desired feature（或等待消费的下一房奖励），连接状态下每 4 秒静默读取一次 status。
+- 游戏在场景切换后自主激活功能时，等待中的橙色 icon 会自动恢复 active 紫色，不再要求手动重新开关一次。
+
+## 左侧存档管理闪动
+
+- “存档管理”按钮不再绑定瞬时 `model.busy` disabled 状态；普通开关、数值和列表修改不会再让该文字反复 disabled/enabled 而闪动。
+
+## 下一房奖励
+
+旧实现仍使用 Early Access `RoomReward*` 名称，已改成当前正式版奖励 ID：
+
+- `RoomMoneyDrop`
+- `MetaCurrencyDrop`
+- `MetaCardPointsCommonDrop`
+- `MemPointsCommonDrop`
+- `MaxHealthDrop`
+- `MaxManaDrop`
+- `StackUpgrade`
+- `WeaponUpgrade`
+
+诸神/塞勒涅等 Loot 使用 `Boon + ForceLootName`，与当前游戏 `SpawnRoomReward` 的原生路径一致。
+
+旧 Profile 中保存的 `RoomRewardMoney / RoomRewardMetaPoint / RoomRewardPsyche / RoomRewardMaxHealth / RoomRewardPom` 会自动迁移；已经不存在安全对应项的旧 `RoomRewardMixerFabric` 会清除而不是继续写入无效 ID。
+
+下一房奖励是一次性状态：Lua 消费后，后续 status 会同步清除持久化 desired 值，避免 UI 或重连时把已消费奖励重新复活。
+
+## 塞勒涅 / 月神巫咒 Spawn
+
+`SpellDrop` 不再用低层 `CreateLoot` 直接造一个外壳对象，而是调用游戏自己的：
+
+```lua
+GiveLoot({ ForceLootName = "SpellDrop", ... })
+```
+
+这样会使用当前 `LootData.SpellDrop` 的完整原生初始化，包括 Selene 交互、送礼能力、`OpenSpellScreen` 和 `PregenerateSpells` setup event。
+
+其他已经工作正常的普通 Loot/Consumable Spawn 路径不变。
+
+## Runtime
+
+Lua revision **19 → 20**，因为 next-room hook 与 Selene spawn 行为发生了实际变化。同 revision reload 仍保持 resident state；旧 revision 会执行 cleanup 后重新加载。
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Components/TrainerStatControls.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Components/TrainerStatControls.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Components/TrainerStatControls.swift	2026-09-17 15:20:37.127940165 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Components/TrainerStatControls.swift	2026-09-17 16:13:48.696679306 +0000
@@ -17,11 +17,11 @@
         TrainerMetricCard(title: title, icon: icon, tint: tint) {
             TrainerLockButton(locked: locked, enabled: editable, action: onLock)
         } content: {
-            HStack(spacing: 6) {
+            HStack(spacing: 2) {
                 TextField("—", text: $current)
                     .textFieldStyle(.plain)
                     .multilineTextAlignment(.center)
-                    .frame(width: 72)
+                    .frame(width: 54)
                     .font(.system(size: 19, weight: .semibold, design: .rounded))
                     .monospacedDigit()
                     .focused(focus, equals: currentField)
@@ -30,7 +30,7 @@
                 TextField("—", text: $maximum)
                     .textFieldStyle(.plain)
                     .multilineTextAlignment(.center)
-                    .frame(width: 72)
+                    .frame(width: 54)
                     .font(.system(size: 19, weight: .semibold, design: .rounded))
                     .monospacedDigit()
                     .focused(focus, equals: maximumField)
@@ -140,11 +140,11 @@
         TrainerMetricCard(title: title, icon: icon, tint: theme.accent) {
             TrainerLockButton(locked: locked, enabled: isEditable, action: onLock)
         } content: {
-            HStack(spacing: 5) {
+            HStack(spacing: 3) {
                 TextField("—", text: $text)
                     .textFieldStyle(.plain)
                     .multilineTextAlignment(.center)
-                    .frame(width: 80)
+                    .frame(width: 58)
                     .font(.system(size: 20, weight: .semibold, design: .rounded))
                     .monospacedDigit()
                     .focused(focus, equals: focusValue)
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Primitives/TrainerCard.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Primitives/TrainerCard.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Primitives/TrainerCard.swift	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Primitives/TrainerCard.swift	2026-09-17 16:13:48.696380899 +0000
@@ -40,16 +40,20 @@
     }
 
     var body: some View {
-        VStack(alignment: .leading, spacing: 10) {
+        VStack(alignment: .leading, spacing: 6) {
             HStack(spacing: 8) {
-                Label(title, systemImage: icon).font(.caption).foregroundStyle(tint.opacity(0.9))
+                Label(title, systemImage: icon)
+                    .font(.caption)
+                    .foregroundStyle(tint.opacity(0.9))
                 Spacer()
                 accessory
             }
-            content.frame(maxWidth: .infinity, alignment: .center)
+            content
+                .frame(maxWidth: .infinity, alignment: .center)
         }
-        .frame(maxWidth: .infinity, minHeight: 82, alignment: .leading)
-        .padding(15)
+        .padding(.horizontal, 14)
+        .padding(.vertical, 10)
+        .frame(maxWidth: .infinity, minHeight: 82, alignment: .center)
         .background(theme.panel, in: RoundedRectangle(cornerRadius: theme.cardCornerRadius))
     }
 }
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Primitives/TrainerToggleControl.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Primitives/TrainerToggleControl.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Core/UI/Primitives/TrainerToggleControl.swift	2026-09-17 15:20:36.486471662 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Core/UI/Primitives/TrainerToggleControl.swift	2026-09-17 16:13:48.696955440 +0000
@@ -39,7 +39,7 @@
         Toggle("", isOn: Binding(get: { isOn }, set: onChange))
             .labelsHidden()
             .toggleStyle(.switch)
-            .controlSize(.small)
+            .controlSize(.regular)
             .tint(theme.accent)
             .disabled(!enabled)
             .help(helpText)
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Hades2Model.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2Model.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Hades2Model.swift	2026-09-17 12:14:46.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2Model.swift	2026-09-17 16:17:31.781530230 +0000
@@ -133,6 +133,7 @@
     @Published private var activationGraceFeatures: Set<String> = []
     private var hotkeys: GlobalHotkeys?
     private var pendingRestoreTimer: Timer?
+    private var featureStateTimer: Timer?
     private var shuttingDown = false
     var canSetFeature: Bool { connected && capabilities["setFeature"] == true && !exiting }
     var canEditDesired: Bool { backendAvailable && !exiting }
@@ -234,6 +235,8 @@
         invalidatePendingMutations()
         pendingRestoreTimer?.invalidate()
         pendingRestoreTimer = nil
+        featureStateTimer?.invalidate()
+        featureStateTimer = nil
         pendingRestoreID = nil
         pid = nil
         runtimeIssue = ""
@@ -361,6 +364,7 @@
         if let value = patch.elements { elements = value }
         if let value = patch.resources { resources = value }
         if let issue = patch.error, !issue.isEmpty { error = issue }
+        updateFeatureStatePolling()
     }
 
     private func applyStat(_ snapshot: Hades2StatSnapshot?, value: inout Double?, locked: inout Bool) {
@@ -429,6 +433,24 @@
         activationGraceFeatures.remove(key)
     }
 
+    /// Runtime hooks can activate/deactivate as the game moves between scenes
+    /// without a GUI command. Poll only while at least one desired feature is
+    /// enabled so presentation state (waiting/orange -> active/purple) follows
+    /// the resident Lua runtime without requiring another manual toggle.
+    private func updateFeatureStatePolling() {
+        let shouldPoll = connected && (nextRoomReward != nil || desiredFeatureKeys.contains(where: desiredFeatureEnabled))
+        if !shouldPoll {
+            featureStateTimer?.invalidate()
+            featureStateTimer = nil
+            return
+        }
+        guard featureStateTimer == nil else { return }
+        featureStateTimer = Timer.scheduledTimer(withTimeInterval: 4.0, repeats: true) { [weak self] _ in
+            guard let self, self.connected, self.backendAvailable, !self.busy, !self.exiting else { return }
+            self.send(.status, title: "同步功能状态", announceSuccess: false)
+        }
+    }
+
     func enqueueMutation(key: String, request: Hades2Request, title: String, delay: TimeInterval = 0.35) {
         mutationScheduler.schedule(key: key, delay: delay) { [weak self] in
             self?.send(request, title: title, coalesceKey: key, announceSuccess: false)
@@ -753,6 +775,8 @@
     private func finishExit(completion: @escaping (Bool) -> Void) {
         pendingRestoreTimer?.invalidate()
         pendingRestoreTimer = nil
+        featureStateTimer?.invalidate()
+        featureStateTimer = nil
         pendingRestoreID = nil
         invalidatePendingMutations()
         shuttingDown = true
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Hades2View.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2View.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Hades2View.swift	2026-09-17 15:20:37.128843739 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Hades2View.swift	2026-09-17 16:13:48.697624172 +0000
@@ -123,9 +123,15 @@
             sectionHeading("局内数据", icon: "gauge.with.dots.needle.67percent")
             Spacer()
             Button { withAnimation(.easeInOut(duration: 0.18)) { statsExpanded.toggle() } } label: {
-                Label(statsExpanded ? "收起" : "展开", systemImage: statsExpanded ? "chevron.up" : "chevron.down")
-                    .font(.caption.weight(.medium))
-                    .foregroundStyle(.secondary)
+                HStack(spacing: 5) {
+                    Text(statsExpanded ? "收起" : "展开")
+                    Image(systemName: statsExpanded ? "chevron.up" : "chevron.down")
+                }
+                .font(.caption.weight(.medium))
+                .foregroundStyle(.secondary)
+                .padding(.horizontal, 6)
+                .padding(.vertical, 4)
+                .contentShape(Rectangle())
             }
             .buttonStyle(.plain)
         }
@@ -711,12 +717,13 @@
             Picker("下一房奖励", selection: $nextRoomRewardChoice) {
                 Text("不覆盖").tag("")
                 Section("常规") {
-                    Text("金币").tag("RoomRewardMoney")
-                    Text("尘灰").tag("RoomRewardMetaPoint")
-                    Text("魂魄").tag("RoomRewardPsyche")
-                    Text("命运丝线").tag("RoomRewardMixerFabric")
-                    Text("半人马之心").tag("RoomRewardMaxHealth")
-                    Text("力量石榴").tag("RoomRewardPom")
+                    Text("金币").tag("RoomMoneyDrop")
+                    Text("尘灰").tag("MetaCardPointsCommonDrop")
+                    Text("魂魄").tag("MemPointsCommonDrop")
+                    Text("骨骸").tag("MetaCurrencyDrop")
+                    Text("半人马之心").tag("MaxHealthDrop")
+                    Text("魔力上限").tag("MaxManaDrop")
+                    Text("力量石榴").tag("StackUpgrade")
                     Text("代达罗斯之锤").tag("WeaponUpgrade")
                     Text("塞勒涅巫咒").tag("SpellDrop")
                 }
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Views/Hades2HostActions.swift /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Views/Hades2HostActions.swift
--- /mnt/data/MacGamingTrainer_v0.17.8/Sources/Hades2/Views/Hades2HostActions.swift	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/Sources/Hades2/Views/Hades2HostActions.swift	2026-09-17 16:13:48.697152495 +0000
@@ -10,7 +10,7 @@
                 model.saveManagerPresented = true
                 model.refreshBackups()
             } label: { Label("存档管理", systemImage: "externaldrive") }
-            .disabled(!model.backendAvailable || model.busy)
+            .disabled(!model.backendAvailable || model.exiting)
             Button { model.openLog() } label: { Label("查看运行日志", systemImage: "doc.text") }
         }
     }
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/VALIDATION_v0.17.9.txt /mnt/data/MacGamingTrainer_v0.17.9/VALIDATION_v0.17.9.txt
--- /mnt/data/MacGamingTrainer_v0.17.8/VALIDATION_v0.17.9.txt	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/VALIDATION_v0.17.9.txt	2026-09-17 16:24:26.916534219 +0000
@@ -0,0 +1,86 @@
+# Mac Gaming Trainer v0.17.9 / Build 32 validation
+
+=== python3 -m py_compile Backend/core/__init__.py Backend/core/adapter.py Backend/core/game_spec.py Backend/core/module_manifest.py Backend/core/protocol.py Backend/core/registry.py Backend/core/server.py Backend/games/__init__.py Backend/games/hades2/__init__.py Backend/games/hades2/adapter.py Backend/games/hades2/catalog.py Backend/games/hades2/command_router.py Backend/games/hades2/config.py Backend/games/hades2/diagnostics.py Backend/games/hades2/localization.py Backend/games/hades2/preferences.py Backend/games/hades2/preparation.py Backend/games/hades2/profile_service.py Backend/games/hades2/save_service.py Backend/games/hades2/schema.py Backend/games/hades2/transport.py Tools/generate_game_binding.py Tools/module_support.py Tools/validate_game_module.py tests/legacy/test_round11_static.py tests/legacy/test_server_round10.py tests/legacy/test_server_round9.py tests/legacy/test_swift_round10.py tests/legacy/test_swift_round9.py tests/run_lua_mock.py tests/test_backend_core_round12.py tests/test_build_contract_round14.py tests/test_global_decoupling_round15.py tests/test_hades2_adapter_round12.py tests/test_hades2_mutation_scheduler_round17.py tests/test_hades2_services_round16.py tests/test_hades2_timeout_policy_v0177.py tests/test_hades2_visual_baseline_v0174.py tests/test_module_contract_round13.py tests/test_packaged_module_round14.py tests/test_preparation_round9.py tests/test_round12_static.py tests/test_round17_1_swift_scope.py tests/test_round17_static.py tests/test_runtime_reliability_round17.py tests/test_swift_declaration_uniqueness_v0176.py tests/test_swift_file_boundaries_round14_1.py tests/test_swift_integrity_v0172.py tests/test_swift_round12.py tests/test_swift_symbol_integrity_v0173.py tests/test_toggle_unification_v0178.py tests/test_ui_component_boundary_v0175.py tests/test_v0179_regressions.py ===
+=== python3 -c import ast; from pathlib import Path; fs=list(Path("Backend").rglob("*.py"))+list(Path("Tools").rglob("*.py"))+list(Path("tests").rglob("*.py")); [ast.parse(p.read_text(), filename=str(p), feature_version=(3,9)) for p in fs]; print("python39_grammar_ok", len(fs)) ===
+python39_grammar_ok 53
+=== python3 tests/legacy/test_round11_static.py ===
+round11_compat_ok
+=== python3 tests/legacy/test_server_round10.py ===
+server_round10_compat_ok
+=== python3 tests/legacy/test_server_round9.py ===
+server_round9_compat_ok
+=== python3 tests/legacy/test_swift_round10.py ===
+swift_round10_compat_ok
+=== python3 tests/legacy/test_swift_round9.py ===
+swift_round9_compat_ok
+=== python3 tests/test_backend_core_round12.py ===
+backend_core_round12_ok
+=== python3 tests/test_build_contract_round14.py ===
+build_contract_round14_ok
+=== python3 tests/test_global_decoupling_round15.py ===
+global_decoupling_round15_ok
+=== python3 tests/test_hades2_adapter_round12.py ===
+hades2_adapter_round12_ok
+=== python3 tests/test_hades2_mutation_scheduler_round17.py ===
+hades2_mutation_scheduler_round17_ok
+=== python3 tests/test_hades2_services_round16.py ===
+hades2_services_round16_ok
+=== python3 tests/test_hades2_timeout_policy_v0177.py ===
+hades2_timeout_policy_v0177_ok
+=== python3 tests/test_hades2_visual_baseline_v0174.py ===
+hades2_visual_baseline_v0175_ok
+=== python3 tests/test_module_contract_round13.py ===
+module_contract_round13_ok
+=== python3 tests/test_packaged_module_round14.py ===
+packaged_module_round14_ok
+=== python3 tests/test_preparation_round9.py ===
+preparation_round8_ok
+preparation_round9_staged_ok
+=== python3 tests/test_round12_static.py ===
+round12_static_ok
+=== python3 tests/test_round17_1_swift_scope.py ===
+round17_1_swift_scope_ok
+=== python3 tests/test_round17_static.py ===
+round17_static_ok
+=== python3 tests/test_runtime_reliability_round17.py ===
+runtime_reliability_round17_ok
+=== python3 tests/test_swift_declaration_uniqueness_v0176.py ===
+swift_declaration_uniqueness_v0176_ok
+=== python3 tests/test_swift_file_boundaries_round14_1.py ===
+swift_file_boundaries_round14_1_ok
+=== python3 tests/test_swift_integrity_v0172.py ===
+swift_integrity_v0172_ok
+=== python3 tests/test_swift_round12.py ===
+swift_round12_ok
+=== python3 tests/test_swift_symbol_integrity_v0173.py ===
+swift_symbol_integrity_v0173_ok
+=== python3 tests/test_toggle_unification_v0178.py ===
+toggle_unification_v0178_ok
+=== python3 tests/test_ui_component_boundary_v0175.py ===
+ui_component_boundary_v0175_ok
+=== python3 tests/test_v0179_regressions.py ===
+v0179_regressions_ok
+=== python3 tests/run_lua_mock.py tests/round9_mock.lua ===
+round9_mock_ok
+=== python3 tests/run_lua_mock.py tests/round10_mock.lua ===
+round10_mock_ok
+=== bash -n build.sh ===
+=== python3 Tools/validate_game_module.py hades2 ===
+hades2: ok
+=== python3 Tools/generate_game_binding.py hades2 /tmp/tmp.mMzF6VTeMj/ActiveGame.generated.swift ===
+=== python3 Tools/generate_game_binding.py hades2 /tmp/tmp.2rHXZYuqDj/ActiveGame.generated.swift ===
+=== swiftc -frontend -parse Sources/App.swift Sources/Core/Host/TrainerGameModule.swift Sources/Core/Host/TrainerHost.swift Sources/Core/Input/GlobalHotkeys.swift Sources/Core/Runtime/BackendClient.swift Sources/Core/Runtime/BackendProcess.swift Sources/Core/Runtime/TrainerBackendSession.swift Sources/Core/UI/Components/TrainerFeatureControls.swift Sources/Core/UI/Components/TrainerListControls.swift Sources/Core/UI/Components/TrainerResourceControls.swift Sources/Core/UI/Components/TrainerStatControls.swift Sources/Core/UI/Components/TrainerStatusControls.swift Sources/Core/UI/Primitives/TrainerBadge.swift Sources/Core/UI/Primitives/TrainerCard.swift Sources/Core/UI/Primitives/TrainerField.swift Sources/Core/UI/Primitives/TrainerMessageBanner.swift Sources/Core/UI/Primitives/TrainerRow.swift Sources/Core/UI/Primitives/TrainerToggleControl.swift Sources/Core/UI/StatusComponents.swift Sources/Core/UI/TrainerShell.swift Sources/Core/UI/TrainerSidebar.swift Sources/Core/UI/TrainerTheme.swift Sources/Hades2/Hades2API.swift Sources/Hades2/Hades2BackendState.swift Sources/Hades2/Hades2Model.swift Sources/Hades2/Hades2Module.swift Sources/Hades2/Hades2Types.swift Sources/Hades2/Hades2View.swift Sources/Hades2/Presentation/Hades2FeaturePresentation.swift Sources/Hades2/Services/Hades2MutationScheduler.swift Sources/Hades2/Services/Hades2ShortcutStore.swift Sources/Hades2/Views/Hades2HostActions.swift Sources/Hades2/Views/Hades2ManagementViews.swift Sources/Hades2/Views/Hades2ViewSnapshots.swift /tmp/tmp.2rHXZYuqDj/ActiveGame.generated.swift ===
+=== python3 -c import plistlib; p=plistlib.load(open("Info.plist","rb")); assert p["CFBundleShortVersionString"]=="0.17.9"; assert p["CFBundleVersion"]=="32"; print("plist_version_ok") ===
+plist_version_ok
+=== python3 -c from pathlib import Path; s=Path("Backend/games/hades2/runtime/hades.lua").read_text(); assert "revision = 20" in s; assert "ForceLootName = \"SpellDrop\"" in s; print("lua_revision_spell_drop_ok") ===
+lua_revision_spell_drop_ok
+=== python3 -c from pathlib import Path; s=Path("Sources/Hades2/Hades2View.swift").read_text(); ids=["RoomMoneyDrop","MetaCardPointsCommonDrop","MemPointsCommonDrop","MaxHealthDrop","MaxManaDrop","StackUpgrade","WeaponUpgrade","SpellDrop"]; assert all(x in s for x in ids); print("next_room_reward_ids_ok") ===
+next_room_reward_ids_ok
+=== python3 -c from pathlib import Path; s=Path("Sources/Core/UI/Primitives/TrainerToggleControl.swift").read_text(); assert ".controlSize(.regular)" in s and ".tint(theme.accent)" in s; print("shared_switch_ok") ===
+shared_switch_ok
+=== python3 -c from pathlib import Path; s="\n".join(p.read_text() for p in Path("Sources/Hades2").rglob("*.swift")); assert "Toggle(" not in s; print("hades_no_direct_toggle_ok") ===
+hades_no_direct_toggle_ok
+
+NOTE: Linux validation cannot perform final macOS SwiftUI/AppKit semantic typecheck/link, codesign, LLDB Python import, or real Hades II attach. build.sh performs target-Mac semantic typecheck before optimized compilation.
+
+VALIDATION_v0.17.9_OK
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/legacy/test_round11_static.py /mnt/data/MacGamingTrainer_v0.17.9/tests/legacy/test_round11_static.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/legacy/test_round11_static.py	2026-09-17 15:02:50.121820202 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/legacy/test_round11_static.py	2026-09-17 16:15:52.360136024 +0000
@@ -4,7 +4,7 @@
 lua = (root/'Backend/games/hades2/runtime/hades.lua').read_text()
 model = (root/'Sources/Hades2/Hades2Model.swift').read_text()
 presentation = (root/'Sources/Hades2/Presentation/Hades2FeaturePresentation.swift').read_text()
-assert 'revision = 19' in lua
+assert 'revision = 20' in lua
 assert 'ActiveProjectileCap = 32' in lua
 assert 'type(CurrentRun.ResourcesSpent) == "table"' in lua
 assert 'statAvailable = statAvailable' in lua
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/round10_mock.lua /mnt/data/MacGamingTrainer_v0.17.9/tests/round10_mock.lua
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/round10_mock.lua	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/round10_mock.lua	2026-09-17 16:16:17.517734033 +0000
@@ -7,7 +7,8 @@
 ResourceDisplayOrderData = { "GiftPoints", "MetaCurrency", "OreFSilver" }
 ConsumableData = {
   RoomMoneyDrop = {}, RoomMoneyBigDrop = {}, RoomMoneyTripleDrop = {},
-  MaxHealthDropSmall = {}, MaxHealthDrop = {}, MaxHealthDropBig = {},
+  MaxHealthDropSmall = {}, MaxHealthDrop = {}, MaxHealthDropBig = {}, MaxManaDrop = {},
+  MetaCurrencyDrop = {}, MetaCardPointsCommonDrop = {}, MemPointsCommonDrop = {},
 }
 LootData = {
   ZeusUpgrade = {}, HeraUpgrade = {}, PoseidonUpgrade = {}, DemeterUpgrade = {}, ApolloUpgrade = {},
@@ -17,9 +18,9 @@
   StackUpgradeTriple = { RequiredPackage = "PomTriplePackage" },
   SpellDrop = { RequiredPackage = "SpellPackage", SetupEvents = { { FunctionName = "PregenerateSpells" } } },
 }
-RewardData = { RoomRewardMoney={}, RoomRewardMetaPoint={}, RoomRewardPsyche={}, RoomRewardMixerFabric={}, RoomRewardMaxHealth={}, RoomRewardPom={}, WeaponUpgrade={}, SpellDrop={}, Boon={} }
+RewardData = { WeaponUpgrade={}, StackUpgrade={}, Boon={} }
 ChooseRoomReward = function(run,room,rewardStoreName,previouslyChosen,args)
-  room.Reward={Name="RoomRewardMoney"}; return "RoomRewardMoney"
+  room.Reward={Name="RoomMoneyDrop"}; return "RoomMoneyDrop"
 end
 ScreenData = { UpgradeChoice = { MaxChoices = 3 } }
 CalcNumLootChoices = function(lootData) return ScreenData.UpgradeChoice.MaxChoices end
@@ -96,6 +97,7 @@
 local castWeaponPropertyCalls, activeCastCount = 0, 0
 local spawnedLoot, spawnedConsumable, grantedTrait
 local loadedPackages, timeScale = {}, 1
+local giveLootCalls, spellSetupCalls, lastGiveLootArgs = 0, 0, nil
 local enemyTimeScale, heroTimeScale, projectileTimeScale = 1, 1, 1
 _elapsedTimeMultiplier = 1
 local chargeSpeeds, chargeCalls, activatedTraitChecks = {}, {}, 0
@@ -263,6 +265,19 @@
   if args.Name=="StackUpgradeTriple" then assert(loadedPackages.PomTriplePackage,"triple Pom package was not loaded") end
   nextObjectId=nextObjectId+1; spawnedLoot=args.Name; return { ObjectId=nextObjectId, Name=args.Name }
 end
+GiveLoot = function(args)
+  giveLootCalls=giveLootCalls+1
+  lastGiveLootArgs=args
+  local name=args.ForceLootName or "ZeusUpgrade"
+  if name=="SpellDrop" then
+    assert(args.AutoLoadPackages==true,"Selene GiveLoot must auto-load packages")
+    loadedPackages.SpellPackage=true
+    if PregenerateSpells then PregenerateSpells() end
+  end
+  nextObjectId=nextObjectId+1
+  spawnedLoot=name
+  return { ObjectId=nextObjectId, Name=name, CanReceiveGift=name=="SpellDrop", OnUsedFunctionName=name=="SpellDrop" and "OpenSpellScreen" or nil }
+end
 local boonOptionGenerationCalls = 0
 GetRarityChances = function() return { Common=1, Rare=0.20, Epic=0.10, Heroic=0.05 } end
 SetTraitsOnLoot = function(loot)
@@ -342,7 +357,7 @@
   timeScale=current
 end
 SetTimeScale = nil
-PregenerateSpells = function() end
+PregenerateSpells = function() spellSetupCalls=spellSetupCalls+1 end
 SilenceForDreamRun = function() end
 AddTraitToHero = function(args) grantedTrait=args.TraitName; return { Name=args.TraitName } end
 
@@ -685,10 +700,10 @@
 chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
 assert(chosen=='Boon' and forcedRoom.ForceLootName=='ZeusUpgrade','next-room god reward override failed')
 M.dispatch('set_next_room_reward',{reward=nil})
-M.dispatch('set_next_room_reward',{reward='RoomRewardMetaPoint'})
+M.dispatch('set_next_room_reward',{reward='MetaCurrencyDrop'})
 forcedRoom={Encounter={Name='Encounter'}}
 chosen=ChooseRoomReward(CurrentRun,forcedRoom,'RunProgress',nil,{})
-assert(chosen=='RoomRewardMetaPoint' and forcedRoom.Reward.Name=='RoomRewardMetaPoint','next-room meta currency reward override failed')
+assert(chosen=='MetaCurrencyDrop' and forcedRoom.Reward.Name=='MetaCurrencyDrop','next-room meta currency reward override failed')
 M.dispatch('set_next_room_reward',{reward=nil})
 
 -- Garden QoL uses the game's own multi-plant and harvest-all paths without
@@ -789,6 +804,18 @@
 M.dispatch('lock_element',{element='Aether',locked=false}); M.dispatch('lock_resource',{resource='Money',locked=false})
 M.dispatch('set_feature',{feature='gameSpeed',value=1})
 
+-- Next-room override uses current full-release reward identifiers. Direct
+-- consumables stay direct, while Selene/god loot goes through Boon+ForceLootName
+-- so the native SpawnRoomReward path calls GiveLoot.
+state=M.dispatch('set_next_room_reward',{reward='MaxHealthDrop'})
+local overrideRoom={Encounter={Name='Combat'}}
+local chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{})
+assert(chosen=='MaxHealthDrop' and overrideRoom.ChosenRewardType=='MaxHealthDrop' and overrideRoom.Reward.Name=='MaxHealthDrop','current direct next-room override failed')
+state=M.dispatch('set_next_room_reward',{reward='SpellDrop'})
+overrideRoom={Encounter={Name='Combat'}}
+chosen=ChooseRoomReward(CurrentRun,overrideRoom,'RoomRewardStore',nil,{})
+assert(chosen=='Boon' and overrideRoom.ChosenRewardType=='Boon' and overrideRoom.ForceLootName=='SpellDrop' and overrideRoom.Reward.ForceLootName=='SpellDrop','Selene next-room override did not use native Boon path')
+
 -- Spawn each supported kind without invoking NPC narrative state.
 state=M.dispatch('spawn_reward',{reward='RoomMoneyDrop',requestId='r-money'})
 assert(spawnedConsumable=='RoomMoneyDrop' and state.applied,'consumable spawn failed')
@@ -798,6 +825,7 @@
 assert(spawnedLoot=='StackUpgradeTriple' and loadedPackages.PomTriplePackage,'triple Pom spawn/package load failed')
 state=M.dispatch('spawn_reward',{reward='SpellDrop',requestId='r-selene'})
 assert(spawnedLoot=='SpellDrop' and loadedPackages.SpellPackage,'Selene spawn/package load failed')
+assert(giveLootCalls==1 and spellSetupCalls==1 and lastGiveLootArgs.ForceLootName=='SpellDrop','Selene spawn bypassed native GiveLoot/setup path')
 state=M.dispatch('spawn_reward',{reward='trait:SupportingFireBoon',requestId='r-artemis'})
 assert(grantedTrait=='SupportingFireBoon','special trait grant failed')
 local duplicate=M.dispatch('spawn_reward',{reward='WeaponUpgrade',requestId='r-hammer'})
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/round9_mock.lua /mnt/data/MacGamingTrainer_v0.17.9/tests/round9_mock.lua
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/round9_mock.lua	2026-09-17 11:34:57.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/round9_mock.lua	2026-09-17 16:20:12.845830008 +0000
@@ -17,9 +17,9 @@
   StackUpgradeTriple = { RequiredPackage = "PomTriplePackage" },
   SpellDrop = { RequiredPackage = "SpellPackage", SetupEvents = { { FunctionName = "PregenerateSpells" } } },
 }
-RewardData = { RoomRewardMoney={}, RoomRewardMaxHealth={}, RoomRewardPom={}, WeaponUpgrade={}, SpellDrop={}, Boon={} }
+RewardData = { WeaponUpgrade={}, StackUpgrade={}, Boon={} }
 ChooseRoomReward = function(run,room,rewardStoreName,previouslyChosen,args)
-  room.Reward={Name="RoomRewardMoney"}; return "RoomRewardMoney"
+  room.Reward={Name="RoomMoneyDrop"}; return "RoomMoneyDrop"
 end
 TraitData = {
   SupportingFireBoon = {}, InsideCastCritBoon = {}, EchoLastRewardBoon = {}, SpellMockTrait = {},
@@ -252,6 +252,13 @@
   if args.Name=="StackUpgradeTriple" then assert(loadedPackages.PomTriplePackage,"triple Pom package was not loaded") end
   nextObjectId=nextObjectId+1; spawnedLoot=args.Name; return { ObjectId=nextObjectId, Name=args.Name }
 end
+GiveLoot = function(args)
+  if args.ForceLootName=="SpellDrop" then
+    loadedPackages.SpellPackage=true
+    if PregenerateSpells then PregenerateSpells() end
+  end
+  nextObjectId=nextObjectId+1; spawnedLoot=args.ForceLootName; return { ObjectId=nextObjectId, Name=args.ForceLootName, CanReceiveGift=args.ForceLootName=="SpellDrop" }
+end
 local boonOptionGenerationCalls = 0
 GetRarityChances = function() return { Common=1, Rare=0.20, Epic=0.10, Heroic=0.05 } end
 SetTraitsOnLoot = function(loot)
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_backend_core_round12.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_backend_core_round12.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_backend_core_round12.py	2026-09-17 15:21:17.315456833 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_backend_core_round12.py	2026-09-17 16:18:39.508039585 +0000
@@ -34,7 +34,7 @@
 
 
 assert HOST_PROTOCOL_VERSION == PROTOCOL_VERSION == 5
-assert APP_BACKEND_VERSION == '0.17.8'
+assert APP_BACKEND_VERSION == '0.17.9'
 assert any(row['id'] == 'hades2' and row['protocolVersion'] == 4 for row in available_games())
 
 context = GameAdapterContext(
@@ -46,7 +46,7 @@
 hello = router.handle({'id':'hello','command':'hello','params':{}})
 assert hello['ok'] and hello['protocolVersion'] == 5
 assert hello['gameID'] == 'fake' and hello['moduleProtocolVersion'] == 7
-assert hello['result']['backendVersion'] == '0.17.8'
+assert hello['result']['backendVersion'] == '0.17.9'
 reply = router.handle({'id':'a','command':'poke','params':{'x':1}})
 assert reply['ok'] and reply['result']['command'] == 'poke'
 assert router.handle({'id':'a','command':'poke','params':{'x':1}}) == reply
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_hades2_visual_baseline_v0174.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_hades2_visual_baseline_v0174.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_hades2_visual_baseline_v0174.py	2026-09-17 15:21:17.315544394 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_hades2_visual_baseline_v0174.py	2026-09-17 16:18:39.508886112 +0000
@@ -30,12 +30,12 @@
 for semantic in ('Hades2', 'godMode', 'boonRarity', 'dormantFeatures', 'CurrentRun', 'WeaponCast'):
     assert semantic not in all_core_ui, semantic
 
-# Shared switch baseline: old purple native small switch, one implementation.
+# Shared switch baseline: old purple native regular switch, one implementation.
 assert 'accent: Color(red: 0.64, green: 0.51, blue: 1.0)' in theme
 assert 'deferred: .orange' in theme
 assert 'Capsule()' not in toggle
 assert '.toggleStyle(.switch)' in toggle
-assert '.controlSize(.small)' in toggle
+assert '.controlSize(.regular)' in toggle
 assert '.tint(theme.accent)' in toggle
 assert '.animation(.easeInOut(duration: 0.16), value: isOn)' in toggle
 assert 'HStack(spacing: 16)' in features
@@ -49,7 +49,7 @@
 assert 'Text(shortcutText)' not in features
 
 # Stat/resource geometry stays on the old baseline.
-assert '.frame(width: 80)' in stats
+assert '.frame(width: 58)' in stats
 assert '.frame(minHeight: 62)' in stats
 assert 'TrainerLockButton' in stats
 assert 'TrainerNumberField(text: $amount' in resource
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_round12_static.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round12_static.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_round12_static.py	2026-09-17 15:02:50.121470138 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round12_static.py	2026-09-17 16:15:52.360054602 +0000
@@ -13,7 +13,7 @@
 manifest = json.loads((root/'Backend/games/hades2/module.json').read_text())
 
 # Hades gameplay behavior retained.
-for token in ('revision = 19','ActiveProjectileCap = 32','nativeMultiCastControlSet','type(CurrentRun.ResourcesSpent) == "table"','statAvailable = statAvailable'):
+for token in ('revision = 20','ActiveProjectileCap = 32','nativeMultiCastControlSet','type(CurrentRun.ResourcesSpent) == "table"','statAvailable = statAvailable'):
     assert token in lua, token
 
 # Game-internal responsibilities are split instead of teaching Core Hades concepts.
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_round17_static.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round17_static.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_round17_static.py	2026-09-17 15:21:17.315501650 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_round17_static.py	2026-09-17 16:18:39.508127526 +0000
@@ -45,7 +45,7 @@
 
 with (ROOT / 'Info.plist').open('rb') as handle:
     plist = plistlib.load(handle)
-assert plist['CFBundleShortVersionString'] == '0.17.8'
-assert plist['CFBundleVersion'] == '31'
+assert plist['CFBundleShortVersionString'] == '0.17.9'
+assert plist['CFBundleVersion'] == '32'
 
 print('round17_static_ok')
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_toggle_unification_v0178.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_toggle_unification_v0178.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_toggle_unification_v0178.py	2026-09-17 15:21:17.315596152 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_toggle_unification_v0178.py	2026-09-17 16:18:39.508765060 +0000
@@ -24,8 +24,8 @@
         switch_files.append(path.relative_to(ROOT).as_posix())
 assert switch_files == ['Sources/Core/UI/Primitives/TrainerToggleControl.swift'], switch_files
 
-# Shared switch is native-small, purple, animated, and never state-colored.
-for token in ('.toggleStyle(.switch)', '.controlSize(.small)', '.tint(theme.accent)', '.fixedSize()', '.animation(.easeInOut(duration: 0.16), value: isOn)'):
+# Shared switch is native regular, purple, animated, and never state-colored.
+for token in ('.toggleStyle(.switch)', '.controlSize(.regular)', '.tint(theme.accent)', '.fixedSize()', '.animation(.easeInOut(duration: 0.16), value: isOn)'):
     assert token in toggle, token
 assert 'Capsule()' not in toggle
 assert 'tint:' not in toggle
diff -ruN '--exclude=__pycache__' '--exclude=*.pyc' '--exclude=CHANGES_v0.17.8_to_v0.17.9.diff' /mnt/data/MacGamingTrainer_v0.17.8/tests/test_v0179_regressions.py /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v0179_regressions.py
--- /mnt/data/MacGamingTrainer_v0.17.8/tests/test_v0179_regressions.py	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/MacGamingTrainer_v0.17.9/tests/test_v0179_regressions.py	2026-09-17 16:18:08.261717812 +0000
@@ -0,0 +1,100 @@
+from pathlib import Path
+import json
+import sys
+import tempfile
+
+ROOT = Path(__file__).resolve().parents[1]
+sys.path.insert(0, str(ROOT / 'Backend'))
+
+from games.hades2.preferences import Hades2PreferenceStore
+from games.hades2 import preparation
+from games.hades2.adapter import Hades2Adapter
+
+view = (ROOT/'Sources/Hades2/Hades2View.swift').read_text()
+card = (ROOT/'Sources/Core/UI/Primitives/TrainerCard.swift').read_text()
+stats = (ROOT/'Sources/Core/UI/Components/TrainerStatControls.swift').read_text()
+toggle = (ROOT/'Sources/Core/UI/Primitives/TrainerToggleControl.swift').read_text()
+host = (ROOT/'Sources/Hades2/Views/Hades2HostActions.swift').read_text()
+model = (ROOT/'Sources/Hades2/Hades2Model.swift').read_text()
+lua = (ROOT/'Backend/games/hades2/runtime/hades.lua').read_text()
+
+# Compact session cards: total min-height is applied after padding, and paired
+# values/suffixes stay visually together.
+assert '.padding(.vertical, 10)' in card
+assert card.index('.padding(.vertical, 10)') < card.index('.frame(maxWidth: .infinity, minHeight: 82, alignment: .center)')
+assert 'HStack(spacing: 2)' in stats
+assert stats.count('.frame(width: 54)') >= 2
+assert 'HStack(spacing: 3)' in stats and '.frame(width: 58)' in stats
+
+# Whole expand/collapse label is one compact hit target.
+assert 'HStack(spacing: 5)' in view
+assert '.contentShape(Rectangle())' in view
+
+# Shared switch is slightly larger but remains the one shared purple native switch.
+assert '.controlSize(.regular)' in toggle
+assert '.tint(theme.accent)' in toggle
+
+# Save manager is no longer disabled/re-enabled by every transient backend request.
+save_line = host[host.index('Label("存档管理"'):host.index('Button { model.openLog()', host.index('Label("存档管理"'))]
+assert 'model.busy' not in save_line
+assert 'model.exiting' in save_line
+
+# Runtime-state polling reconciles autonomous scene activation/deactivation.
+for token in ('featureStateTimer', 'updateFeatureStatePolling()', 'withTimeInterval: 4.0', 'self.send(.status, title: "同步功能状态", announceSuccess: false)'):
+    assert token in model, token
+
+# Next-room UI and Lua both use current reward IDs, not early-access RoomReward* IDs.
+for token in ('RoomMoneyDrop','MetaCurrencyDrop','MetaCardPointsCommonDrop','MemPointsCommonDrop','MaxHealthDrop','MaxManaDrop','StackUpgrade','WeaponUpgrade','SpellDrop'):
+    assert token in view, token
+for old in ('RoomRewardMoney','RoomRewardMetaPoint','RoomRewardPsyche','RoomRewardMixerFabric','RoomRewardMaxHealth','RoomRewardPom'):
+    assert old not in view, old
+assert 'room.ChosenRewardType = rewardType' in lua
+assert 'rewardType, forceLoot = "Boon", wanted' in lua
+
+# SpellDrop is the special case that must use the game's native GiveLoot path.
+assert 'if rewardId == "SpellDrop" then' in lua
+assert 'requireFunctions("Selene loot spawning", { "GiveLoot" })' in lua
+assert 'GiveLoot({ ForceLootName = "SpellDrop"' in lua
+assert 'revision = 20' in lua
+
+# Old stored preferences migrate forward instead of silently retaining dead IDs.
+normalized = Hades2PreferenceStore.normalize({'nextRoomReward':'RoomRewardMoney'})
+assert normalized['nextRoomReward'] == 'RoomMoneyDrop'
+normalized = Hades2PreferenceStore.normalize({'nextRoomReward':'RoomRewardPom'})
+assert normalized['nextRoomReward'] == 'StackUpgrade'
+normalized = Hades2PreferenceStore.normalize({'nextRoomReward':'RoomRewardMixerFabric'})
+assert normalized['nextRoomReward'] is None
+
+# Once Lua consumes a one-shot override, a clean status must clear persistence
+# instead of resurrecting it from the desired-state overlay.
+class StatusTransport:
+    pid = 4242
+    last_duration = 0.0
+    def alive(self): return True
+    def execute(self, code):
+        return json.dumps({
+            'connected': True, 'status': 'ready', 'scene': 'run',
+            'capabilities': {}, 'desiredFeatures': {}, 'activeFeatures': {},
+            'dormantFeatures': {}, 'featureErrors': {}, 'resources': [],
+            'rewards': [], 'stats': {}, 'statSupport': {}, 'statAvailable': {},
+            'elements': [],
+        })
+    def detach(self): pass
+    def close(self): pass
+
+old_data = preparation.DATA
+try:
+    preparation.DATA = Path(tempfile.mkdtemp(prefix='mgt-v0179-one-shot-'))
+    adapter = Hades2Adapter(transport=StatusTransport())
+    adapter.preferences = adapter._default_preferences()
+    adapter.preferences['nextRoomReward'] = 'MaxHealthDrop'
+    adapter.preference_initialized = True
+    adapter.preference_dirty = False
+    adapter.state.update(connected=True, status='ready', nextRoomReward='MaxHealthDrop')
+    state = adapter.execute('status', {})
+    assert adapter.preferences['nextRoomReward'] is None
+    assert state['nextRoomReward'] is None
+finally:
+    preparation.DATA = old_data
+
+print('v0179_regressions_ok')
